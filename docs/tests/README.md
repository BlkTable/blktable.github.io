# Tests

There is no build step and Node is not installed standalone on the dev machine,
so the tests are plain scripts run with the Node that ships inside VS Code:

```bash
cd C:/Users/ASUS/blktable
ELECTRON_RUN_AS_NODE=1 "C:/Users/ASUS/AppData/Local/Programs/Microsoft VS Code/Code.exe" \
  docs/tests/shell-helpers.test.js
```

The exceptions are the `*.chrome.js` files, which drive the real thing in headless Chrome;
each finds Chrome or Edge itself, and skips instead of failing when neither is installed.

Run them from the repo root — each one reads `index.html` and pulls the functions
it tests out of the page by name, so there is nothing to keep in sync by hand and
a renamed function fails loudly instead of silently going untested.

| file | covers |
| --- | --- |
| `shell-helpers.test.js` | Home's recently-opened grouping (`periodOf`, `groupByPeriod`, `agoText`), per-table colour and glyph (`tableTint`, `tableGlyph`), and the filter engine's condition groups (`passesList`, `filterCount`, `pruneConds`, `condSlot`) |
| `conditional-questions.test.js` | "ask this only if" (`condMet`, `condLabel`) — and it loads `condMet` from **both** `index.html` and `f/index.html`, asserting the same answer from each, because the public form and the review panel each carry a copy of the rule |
| `flag-tag.test.js` | the red "needs noticing" mark (`flagTagHtml`) — which answers trip it, that a blank answer never does, that matching ignores case, and that the admin-typed label is escaped |
| `payroll.test.js` | the date-range export (`payrollRows`, `payrollConfig`, `inDateRange`, `payrollNumber`) — the output is money somebody is paid, so every test is either "a row was quietly dropped" or "money went out twice": both ends of the range inclusive, an event with no date never in range, a rate that is not a number reading as 0 rather than NaN, a backup who never worked not paid, a signup whose event was deleted earning nothing, `Ahmad`/` ahmad ` counted as one person while genuinely different spellings stay separate (as decided), and a nameless signup kept as `(no name)` rather than dropped |
| `swap.test.js` | moving somebody between confirmed and backup (`slotPillHtml`, `slotActions`, `slotCountText`) — that a table where nothing holds a place is untouched, that confirmed and backup differ by colour and not only wording, that the pill's classes actually exist in the stylesheet, that only the move you are *not* on is offered and only to a manager, and that a place move is intercepted before the WhatsApp/Call/Email dispatcher it borrows its menu from. The two rules that protect the count — the trigger promoting the oldest backup on delete *only*, and a promotion into full places being refused under a lock — are database behaviour and are checked in `13-swap-and-promote.sql` |
| `one-per-browser.test.js` | one submission per browser (`alreadyText`, `deviceKey` from `f/index.html`) — that a backup is never told they have a place, that the key is minted only for a form that opted in, that `p_device` is added to the payload conditionally (a fourth key against a three-argument function is a dead submit button on every form), and that a browser refusing storage returns no key and submits anyway rather than locking a real person out. It pulls `DEVICE_STORE` out of the page too, so the test cannot use a different storage key than the page does |
| `event-phase.test.js` | the status that is worked out rather than pressed (`eventWindow`, `eventPhase`) — that a table with no declared window is untouched, that done beats filled (a finished event must not read as still coming) and running beats both, that an empty Places box means no limit rather than instantly filled, that an end time before its own start is treated as the day's end instead of marking the event over before it began, and that bad input never produces a phase that reads as closed. The rule also exists in SQL (`event_time_phase`); the file records the one case where the two deliberately differ and why |
| `capacity.test.js` | capacity and backup places (`slotsText` from `f/index.html`) — that a form with no capacity says nothing at all, that places count down and read as singular at one, that a backup is told plainly they would be on the backup list rather than have a place, that a closed event says *closed* rather than *full*, and that an over-booked capacity never shows a negative number. The counting itself is done in the database under a row lock and is covered by the checks in `11-capacity-and-backup.sql` |
| `parent-links.test.js` | parent-scoped form links (`recordFormLink`, `childTableOf` from `index.html`, `fmtParentValue` from `f/index.html`) — that a record link is the ordinary form link plus an encoded token on the same page, that the child table is found from the children rather than a pointer on the parent, and that a date on the public page reads as a date but an impossible one (31 February) is shown as recorded instead of silently rolling over to 3 March |
| `new-record.test.js` | adding a record by hand (`newRecordVisible`, `newRecordMissing`, `newRecordData`) — that a question which does not apply carries no answer, that an empty answer stores no key, that only questions being asked can be required, and how the free text behind an "other" choice is kept or dropped. This path writes straight into `app_submissions` with no form page to eyeball, so a row it builds wrongly lands in a live table |
| `archive.test.js` | archiving a table instead of deleting it (`isArchived`, `archivedConfig`, `archiveConfirmText`) — that the flag the 219 imported tables already carry reads the same as one set from the menu, that archiving rebuilds the config without disturbing columns, stages, capacity or parent, that it never mutates the object the page is still holding, that restoring leaves no `archived_*` key behind so a round trip is a no-op, and that the confirm says the three things a person needs before pressing it (nothing is deleted, it can be undone, the public link keeps working). The last few read `index.html` as source: both menus must confirm before archiving, neither may confirm before restoring, the write is gated on `isAdmin`, and `config.archived` may appear raw exactly once — a second hand-written copy is how a table gets hidden from the list but missed by the Archived count |
| `media-field.test.js` | the `media` question type — a question that takes a photo **or** a video (`isFileField`, `isVideoPath`, `isPlayableVideo`, `fileLabel`, `coverHtml`, `photoSectionHtml`, `uploadCap`, plus `isVideoFile`/`sizeText` from `f/index.html`). What the answer *is* has to be decided from the stored R2 key alone, because no MIME type is stored beside it: that a key with no extension reads as an image (every row written before this existed), that `holiday.mp4.jpg` is a JPEG rather than a video, that `.avi` is a video but is offered as a download rather than handed to a player that would show a black rectangle, that a video never becomes an `<img>` or a card thumbnail (there is none — the sweeper builds them from images), and that the gallery heading counts videos honestly instead of calling them photos. The two ceilings live in both pages, the way `condMet` does, so the last tests read both files and fail if only one is raised |
| `delete-selected.test.js` | deleting a ticked selection from the grid (`deleteSelectedRecords`, `paintSelBar`) — the one test file here that tests a **caller** rather than a helper, because `payroll.test.js` passing 16/16 while the export was broken proved that a helper tested in isolation says nothing about who calls it and with what. The database, the R2 bucket, the confirm dialog and the DOM are stubbed and the assertions are about the calls made and their **order**: that a parent's children are deleted *before* the parent (the FK is `ON DELETE SET NULL`, so the other order does not delete them, it orphans them — `parent_id` null, invisible in every view, worth nothing to the payroll export), that a failed child delete stops before the parents are touched, that every file field's upload is removed from the bucket and not just the first, that files go before rows, that 250 ids become chunks of 100/100/50 with nothing lost or repeated, that a reviewer who cannot manage deletes nothing even if the handler is called directly, that cancelling deletes nothing including the files, that the confirm names the child count and pluralises it, that a count the database refuses errs toward warning rather than promising nothing will be lost, and that success clears the ticks and re-reads the table rather than splicing the array and leaving the entry count and footer totals stale |
| `card-customise.test.js` | "Customize cards" — which file question is a card's cover, whether it is cropped or fitted, which fields the card carries and in what order, and whether their names show (`cardPrefs`, `summaryFields`, `cardChoosable`, `cardSave`, `coverHtml`). The rule has three levels and nearly every test is about which one wins: this browser (localStorage) over the table's saved default (`app_tables.config`) over the behaviour every card had before the panel existed. The first test is the one that matters most — with nothing configured and nothing chosen a card must draw exactly as it does today, or the feature is a silent redesign of 226 live tables — and the rest are the ways a choice goes stale or reads wrongly: a cover naming a question since deleted, a cover naming a question that is no longer a file at all, an empty field list read as "nothing chosen" rather than as an empty card (the first chosen field *is* the title), `config.card_no_cover` still meaning no cover on the tables that carry it, and "None" surviving despite being stored as `""` — falsy, so anything testing the value instead of the key hands the cover straight back. The last few read `index.html` as source: a helper nobody calls is a feature nobody has, so they assert the cards read the chosen cover and shape, that field names are switchable, and that "Save for everyone" is gated on `isAdmin` |
| `card-panel.chrome.js` | the same panel, **clicked**: the arrows, the tick boxes, Crop/Fit, the cover radios and Reset, driven in order with the answer read back out of storage. The only file here that needs more than node — headless Chrome, found automatically or named with `CHROME=<path>`, and **skipped rather than failed** when there is none — because reorder and "the last field cannot be unticked" are DOM and events, and a panel whose arrows quietly stopped working would pass every other test in this folder. It lifts the stylesheet and the functions out of `index.html` and stubs `renderCustom` to redraw the panel, which is what the app's own render does |
| `sidebar-groups.test.js` | the sidebar's left edge (`groupByCategory`, plus the two fold headings read out of `index.html` as source). Two rules, both of which arrived as "why do these all look different?" rather than as a bug report about grouping: **a group of one is not a group** — the Airtable migration made one category per base and 37 of them hold a single table, so the sidebar showed a fold arrow, a name and the count "1" and opened onto one table usually called the same thing — and **every row starts at the same x**, which needs the fold arrow in the same 20px slot a table's colour mark occupies instead of as text inside the label. The tests that matter are the ones about *not* losing anything: a flattened table keeps its place in the list rather than being appended after everything else, the category is still on the row so it groups again by itself the moment a second table joins it, and the width of `.side-fold` is compared against `.tmark` in the stylesheet so the two cannot drift |
| `duplicate-table.test.js` | duplicating a table (`duplicate_table` is a database function that verifies itself when applied — see `workspaces/19-duplicate-table.sql`; these test the page). Every test is a way the feature could ship looking finished and not be: the item missing from one of the two ⋯ menus, which are separate code built two separate ways; a non-admin being offered it; the copy being made but never opened, so it is lost in a sidebar of 272 entries; "copy the records too" defaulting to on, which for the biggest table copies 28,000 rows because somebody clicked through a dialog; and a missing migration reading as a permissions problem, which is exactly what `record_share_token` did for a week |
| `linked-records.test.js` | linked-record questions in the form editor (`linkRecordOptions`, `optsToString`, plus the save path read out of `index.html` as source). **Two different questions share the type `link`** — a link *button* the builder makes (`{url, text, text_ar}`, three of them) and a linked *record* the Airtable importer made (`{links_to_name, links_to_table}`, two hundred of them across 103 tables) — and the editor only ever knew about the first. `optsToString` returned `""` for a linked record (its options object has neither `.list` nor `.url` and no `.length`), so the Options box came up empty, and on save the `link` branch demanded a URL out of that empty box, failed the `http://` check and **returned — abandoning the save of the whole table**, with an error about a URL for a question that has nothing to do with URLs. The tests are the two halves of that: the metadata survives a round trip through the editor and goes back untouched, and it is never mistaken for a URL button — including a half-written one that has lost its name, which must still count as a linked record rather than falling through to the URL rule. Three assertions read the save as source, because the order matters: the linked-record check has to run *before* the URL rule or it still rejects the save first |
| `builtin-public-title.test.js` | the heading `/apply` and `/cast` print, which is **not** the table's name (`builtinPublicTitle`, `serializePublicTitle`, `setPublicTitleInputs`, plus `applyHeading` read out of both public pages). The table is called "Job Applications"; the page says "Job Application · طلب توظيف" — so renaming the table could never reach the page, and pretending it did would have shown applicants a heading they never see. The heading is its own setting (`config.title`), carried by `get_form_config`, which returns the built-in table's config as it is. Most of the tests are about **not** breaking the pages that have no title set — which is all of today's traffic: no title, a `null` title (what two empty boxes actually store) and a whitespace-only title must all leave the served heading exactly as it was. The rest are the ways a bilingual heading goes wrong: each half falls back on its own so setting only the English cannot blank the Arabic, two empty boxes store `null` rather than an empty object that reads as "set", the markup keeps its own heading so the page reads correctly before the request completes and if it fails, and the text is set with `textContent` and never `innerHTML` — it comes from the database and is typed by a person. The rule exists three times (the dashboard preview and both pages) the way `condMet` does, and the tests load all three so they cannot disagree about what the applicant is looking at |
| `form-live.test.js` | whether a public form's link is live — `app_tables.is_active` (`linkLive` from `index.html`, plus `setFormLive` and the `f/index.html` lookup read as source). **Recovered from `design/airtable-form-parity`, where it was written and never merged**, while the feature it covers has been live since #37 — which shipped with no tests. This is the flag behind the worst bug the project has had: nothing in the app ever wrote it, so on 2026-08-17 the Contact Us link answered "Form not found" on launch day along with **176 other form tables**, while the Form tab drew a link and a QR code for every one of them. The tests cover the deliberate asymmetry in `linkLive` — a **missing** flag counts as LIVE, because a row loaded before the column was selected must not be reported as a dead link — that the Postgres text `'f'` reads as live (a guard on the shape of the data, since `'f'` is truthy in JS), that the flag is read in one place rather than hand-copied, that the write is scoped to one table id (a bare update would publish or unpublish every form at once), that switching a form **off** is confirmed while switching it on is not (off is the direction where a printed QR code silently stops working), and that the public page still filters on the same flag — if it stops, the switch in the app becomes decoration. Two assertions were rewritten on recovery because they pinned spellings main has since improved on: the write gate widened from `isAdmin` to `mayModifyTable` (admin **or** the table's creator), and the not-found guard now routes through `resolveAliasOrNotFound` so a retired slug forwards instead of dying; both now assert the rule and were re-proved to catch an ungated switch and an unfiltered lookup |
| `form-country.test.js` | which country a form is for, and the franchise family (`builderConfig`, `fieldRowsFor`, `tableCountry`, `isFranchiseParent`, `branchListMismatch`, plus the create path, `writePendingConds`, `setBuilderChrome` and the Countries manager read out of `index.html` as source). The thing being undone here is worth stating: a `Country` question was appended to every new table and back-filled onto all 260, so **260 forms asked the public which country they were in and 4 records ever answered** — 10 of the 14 answers say "Jordan". The country is a property of the form, so the first test is that nothing appends one any more and that exactly **one** place in the page still writes a country field: the parent of a franchise family, where asking is the point. The rest are the ways the family could go wrong quietly — the parent's country question not first or not required, the designed questions not shifted down to leave room for it (and `writePendingConds` not shifted with them, which is a conditional question gated on the answer to a *different* question, invisible because both pages would agree and both be wrong), a sub-form that does not record its own country or point back at its parent, five inserts fired at once against one slug prefix, and a half-created family reporting "could not create" while three real forms with real links already exist. The Countries manager's tests are all "a shop is never deleted": its name is the answer on every record ever filed against it, so closing it is a flag, a rename to nothing is refused, a move between countries is confirmed, and a duplicate name says whether the namesake is merely switched off |
| `no-application-history.test.js` | that the second Job Applications sidebar entry is **gone** and cannot come back (`jaScoped`, `loadFacets`, `openJa`, `paintBuiltinNames`, plus the markup and the sidebar handler read as source). `origin` split one table into two entries and the imported side was where **33,789 real applicants sat unread**; `workspaces/31` moved all of them onto the board, so the entry listed nothing. Every test is source-read on purpose, because what was removed is *reveal* code — a sidebar row, a scope default, a name suffix — and this project has shipped three features that were live and unreachable precisely because nothing tested the reveal. The scope is asserted **absent rather than defaulted**: a filter matching every row can only ever hide one by accident, and with no entry left there would be nowhere to find what it hid. The one that matters most is the RPC body — `p_origin` must still be sent, as `null`, because PostgREST resolves a function by the keys in the body and dropping one turns a three-argument call into a two-argument call that may resolve to nothing, which is the shape of the 2026-08-09 outage and the `p_token` near-miss |
| `ballot-field.test.js` | the ballot — a `record_multi` question whose choices are another table's live rows (`ballotLabel`, `ballotOptions` from `f/index.html`, `ballotNames` from `index.html`). The test that matters is about the stored **value**: it is the record id and never the printed name, so renaming an event cannot orphan a vote — the same rule that stops every other config in this app matching questions by label. The rest are the ways an empty ballot reads: no events, a failed RPC, and a database that does not have `ballot_options` yet must all render the form with a "nothing open" line rather than a blank box or a dead page, because the page and the migration have to be deployable in either order (the `p_token` lesson). A dateless event sorts last rather than heading the list, matching what `ballot_options` already does in SQL. On the display side, ids are exact storage and useless to read: `ballotNames` resolves them, and an id whose event was deleted is **named** as deleted rather than dropped, since a vote for a deleted event must not look like a vote nobody cast. Two assertions compare a label against the same event without a rate rather than grepping for digits — `2026-09-10` contains both a 20 and a 10, so a digit search would have tested nothing |
| `poll-links.test.js` | one link per poll, and the picker that fills it (`recordOptsLabel`, `edChecksKeyed`, `linkLive` from `index.html`, plus the three link gates and the ballot payload read as source). A poll is a record, so the link machinery is Standard 1's and the only new UI is a question type: `record_multi`, whose checkbox **value is a record id** and whose text is a label built from the source table's own `card_fields` — the first of which is already that record's title everywhere else, so this cannot drift from what the cards show and nothing looks a question up by its label. A ticked id whose record is gone stays shown, stays ticked and is marked, the same rule `edChecks` follows for a retired choice and for the same reason: dropping it means saving quietly deletes an answer nobody removed. It trims where `edChecks` does not, because `splitMulti` only eats whitespace next to a comma and these values are ids, where a stray space means the box does not tick and the id is lost on save. The rest are the ways this ships looking finished and is not: `edValues` must route `record_multi` through `edChecksValue` or the picker renders, ticks, and saves nothing; the source records must load before an editor is drawn or the picker is empty; and the three per-record link sites must all gate on `linkLive(child)` — without it every event offered a link and a QR code for the **roster** form, which is switched off and which nobody fills in over the web. The last test is the 2026-08-09 rule: the poll token is added to the `ballot_options` body only when there is one, because PostgREST resolves a function by the keys in the body |
| `assign.test.js` | the roster panel's reading half (`assignConfig`, `assignPersonKey`, `assignCandidates`, `assignMonthCount`, `eventsOverlap`, `assignClashes`). The rule Zamel asked for is one test: somebody who voted for A and B is **not** offered for C. Around it are the ways that goes quietly wrong — `e-1` matching `e-12` by substring, and a barista who votes again being listed twice instead of replacing their earlier answer (**the latest vote decides**, so withdrawing a tick actually withdraws it, and array order never decides — `created_at` does). Identity is `trim().toLowerCase()`, the same rule `payrollRows` uses: if the two ever disagree, the roster and the money disagree about who somebody is. Votes cast under different poll links both count, because the tick names the event, not the poll. The clash tests pin the half-open reading of a shift: 10–16 and 16–20 are two shifts one person can work, a missing end time is the end of the day, and an end before its own start is too — which is what `eventPhase` already does, and two rules about one pair of times that disagree is how an event reads as over before it began |
| `assign-submit.test.js` | the **caller** that writes a roster (`assignDiff`, `submitAssignments`), database stubbed, assertions about the calls made and their order. `payroll.test.js` passing 16/16 while the export was broken is why this file exists. The one test the feature lives on: **a re-submitted roster with no change must not touch the database at all** — every write is a queued message, so a careless save re-messages the whole team and the feature is switched off in week one. Beside it: a slot change is an `update` and never a delete-plus-insert (which would re-fire the INSERT-only trigger and tell somebody twice), a missing slot counts as confirmed on both sides so old rows do not all look changed, a duplicate row for one person is trimmed to one rather than re-added, inserts go **last** so nothing is messaged before the removals land, and a failed delete stops before anything is inserted. Two assertions are about where slot lives: it is written as the **native `app_submissions.slot` column**, because `payrollRows` filters `s.slot` — a slot inside `data` leaves that column null, drops every row from `only_slot:'confirmed'`, and makes every export read "nothing in that range", which is how the export broke on 2026-08-17. The rest read the page as source: the button follows `can_manage` like Payroll, `openAssign` re-checks it so the gate does not live only in the markup, submitting never touches the event's status, and the write never enforces capacity |
| *(multi-day, folded into the three files above)* | an event can span days (`eventDays`, `ballotKey`, `assignRowKey`, plus the day arm of `assignCandidates`, `assignDiff`, `assignMonthCount`, `assignClashes` and `ballotNames`). A vote names one **day** of an event, stored as `<id>#<date>`, so a barista can take the Friday of a three-day fair and not the Saturday — and the tests that matter are the ones where days must not bleed: `e-9#Friday` is not matched by a Saturday query, a day-bearing vote is not matched by a dayless one, and a person who ticked Friday and Sunday is offered on neither Saturday. On the roster the identity is **person + day**, not person: keyed on the person alone, Day 2's row reads as a duplicate of Day 1's and saving Day 2 deletes Day 1. One row per person per day is also what makes the money right with no payroll change — `payrollRows` adds the event's rate once per row, so two days of a 15 JD event pays 30. `eventDays` is pinned on the ways a range goes wrong: inclusive at both ends, across a month, across a year, across a leap day, an end **before** its start reading as one day rather than zero, no date at all reading as one unnamed day so a dateless event stays tickable, and a mistyped year capped at 60 rather than drawing hundreds of boxes. `ballotNames` splits the day back off, because looking the whole key up as an id would print "(deleted event)" against every good vote |
| `access-picker.test.js` | the one searchable list behind both "which tables can this person open" (New/Edit user) and "who can open this table" (Share) — `accessLevelOf`, `accessGrantFor`, `pickerMatch`, `pickerRows`, `pickerCountText`, `newTempPassword`, `inviteText`. Before it, both modals printed every row with three tick boxes and no search: granting one table out of 118 meant scrolling 118 rows. Most of the tests are about the two places a wrong answer changes somebody's permissions. First, what a stored grant reads as — including the boolean read back as the text `'f'`, which is truthy in JS and would promote every viewer to a manager. Second, and the one that matters: collapsing three tick boxes into one level makes "manager" imply "can edit", which is what the tooltip always said but not what the boxes enforced, so **a row the admin never touched is written back exactly as it was stored** rather than re-derived — otherwise opening the modal to fix somebody's name silently hands every manager-without-edit row the edit right nobody granted. The rest: a search that reads the Arabic name and the email as well as the name, an order that puts what is already granted first while leaving the sidebar's order alone underneath, a temporary password with no `l`/`I`/`1`/`O`/`0` in it because it is read off one screen and typed on another, and an invite that carries the address, the email and the password together. The last few read `index.html` as source, because a helper nobody calls is a feature nobody has: both modals go through `accessPicker`, the old per-row classes are gone rather than left beside it, and creating an account no longer ends in the `window.alert` that told the admin to share a password it never showed them |
| `access-picker.chrome.js` | the same picker, **clicked**: typing in the search box, ticking a row, changing a level, Select all, Clear, and the country line that appears with a grant rather than sitting greyed out on all 118 rows. Skipped rather than failed when there is no Chrome, like `card-panel.chrome.js`. The tests that need a browser are the ones about what the list does *while* somebody is using it: a search that narrows to what matches (and says so when nothing does), a count that keeps naming the whole list rather than the visible part, a row that does **not** jump to the top the moment it is ticked — reordering happens on a new search, which is the one moment nobody is mid-click — Select all adding only what the search is showing, and unticking a row putting its level back to view instead of keeping the old answer where it cannot be seen. It re-proves the untouched-row rule through the real DOM, since that is where the modal actually reads it from |
| `multi-upload.test.js` | more than one file on one upload question (`filePaths`, `fileValue`, `firstPath`, `coverPath`, `recordFilePaths`, `groupUploads`, `photoSectionHtml`, `customCellText`, plus `filePaths`/`fileValue`/`groupUploads`/`MAX_FILES` from `f/index.html`). Almost every test here guards the same thing from a different side: an answer written before this existed is a single bare key, all 187,791 of them, so **one file stores as a plain string and only several store as an array** — `fileValue(filePaths(v)) === v` on every old shape, a lone photo rendering identically to an array of one, a lone photo keeping its bare label while several get numbered `(2/3)`, and a one-file grid cell emitting exactly the markup it always did with no wrapper. The rest are the ways multiplicity goes wrong: the gallery heading counting files rather than questions, the header photo being skipped **once** rather than hiding the other nine photos of its question (the bug the gallery was added to fix, one level down), the skip taking an image rather than the first file blindly so a video is not swallowed, an export naming every file rather than the first, `groupUploads` keeping two questions apart and preserving the order somebody chose their photos in — asserted against **both** pages, because assigning each path straight onto `data[id]` uploads ten photos successfully and then keeps one. The count ceiling is asserted equal in both pages for the same reason the byte ceilings are, and a file still answering a second question is not deleted from the bucket, because `uploadAll` remembers per File object and one photo on two questions is one key |
| `form-draft.test.js` | a half-filled public form surviving a refresh (`draftKey`, `draftFresh`, `readDraft`, `writeDraft`, `clearDraft`, `draftAnswers`, `restoreAnswers`, `splitPhone`, `sweepDrafts` from `f/index.html`, and `draftFormAnswers`/`restoreFormAnswers` from `apply/` and `cast/`). Answers used to live only in the DOM, so anything that reloaded the page emptied it. The tests are not about "it saves" but about the ways a kept draft can do harm: a stale one filling a form somebody thought was fresh (a week is the limit, and a future timestamp from a wrong clock is refused rather than trusted forever), one written before anything was typed, one that survives the submission — a shop tablet is one browser used by everybody — and a browser refusing storage taking the form down with it. Also that a file is never written to storage and never restored, that an answer whose question is gone is dropped rather than guessed at, that a numeric `0` is an answer, and that a phone number splits back into the same picker and box it came from. The last few read the pages as source: the two engines must carry the same key prefix and the same week, both must save on `visibilitychange`/`pagehide`, both must clear on success, and **every field type that can take an answer back must define `setDraft`** — a new type added without one loses its answer on every refresh, silently |
| `form-draft.chrome.js` | the same thing **in a real browser**, twice: a custom `/f/` form and the job application. None of it is reachable from node — it is the page booting, a combobox settling on a choice in code rather than through a DOM event, a country picker, ticked boxes, a real `File` in a file input, and then the whole form drawn again from scratch the way a reload draws it. It checks that every kind of answer comes back, that a question asked only behind a Yes is asked again *with* its answer (it is hidden while the form is being rebuilt), that the photo does not come back and the person is told so in both languages, and — the trap on the job application — that the Country question, which blanks City and "Are you Lebanese?" for a country they do not apply to, cannot wipe those two answers on the way back in. It ends on the rule that protects the next person in the queue: submitting clears the draft, and a page-hide save after that does not write it back. Skipped rather than failed when there is no Chrome |
| `upload-tiles.test.js` | seeing what you attached and taking one back off, on the public form (`fileKey`, `addFiles`, `fileKind`, `extLabel`, `isVideoFile` from `f/index.html`). The reason this exists at all is a browser fact: the control used to read `fin.files`, and a `FileList` is read-only — nothing can be taken out of it and a second pick REPLACES it, which is why a photo attached by mistake could not be removed and why "Change photos" silently lost the rest. So the page owns a plain array, and these are its rules: a second pick **adds** rather than replaces, the same file picked twice is not attached twice (a phone picker hands back a selection that is still ticked), two genuinely different photos sharing a name and a size both survive because identity includes the moment, the list is trimmed to `MAX_FILES` at pick time with the refused count reported rather than the form being blocked, and `addFiles` does not mutate the list it was given. `fileKind` reads the MIME type first and the **extension** when there is none, because Android's picker and several in-app browsers hand over a `File` with `type === ""` and a photo with no preview is the exact failure the tiles exist to fix — which is also why `isVideoFile` now answers through `fileKind`, so the size hint offering "a shorter clip" and the tile drawing a play badge can never disagree about one file. The rest is asserted by source, the way the widgets always are: the input's own list read once and only on its way into `addFiles`, the input emptied after each pick (or a removed photo can never be chosen again — picking it fires no `change` at all), a remove that finds its file by identity rather than trusting a position, `revokeObjectURL`, and the button no longer saying "Change" |
| `upload-tiles.chrome.js` | the same question, **used**: real `File` objects put into the real `<input type="file">` through a `DataTransfer`, the ✕ clicked on the **middle** tile, and the answer read back out of the control — `files()`, which is what Submit uploads. That last step is the whole point: tiles disappearing from the screen while the submission still carries the removed photo would look fixed and be broken, and removing by position instead of identity takes the wrong photo off (verified — it fails four of these). Also covers what node cannot see: three photos drawing three tiles that each show a `blob:` picture rather than a filename, a removed photo being attachable again, a re-picked photo saying "already attached" instead of looking like the click did nothing, emptying the question putting the button back to "Choose photos" and a required question back to unanswered, a 13-photo pick trimmed to ten with the first ten kept and **still a submittable answer**, one oversize file marked on its own tile while its neighbour is not — and removing it clearing the refusal, which is the fix a person can now actually carry out instead of choosing everything again from scratch — and a `file` question badging a PDF rather than drawing a broken picture. Skipped rather than failed when there is no Chrome, like the other `*.chrome.js` files |
