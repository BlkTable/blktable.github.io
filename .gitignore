# This repo is PUBLIC (GitHub Pages). Anything committed here is served at
# https://blktable.blk.jo/<path> and is publicly readable. Keep internal
# operational data out of it.

# Operate Solution export + generated import SQL: contains every internal
# checklist question and the full branch list. Local only.
operate-import/

# Operational DB SQL and incident/outage handoffs. These name internal tables,
# functions and server details and must never be served at blktable.blk.jo.
# Applied SQL lives in the private ~/Documents/blktable-migration folder.
*.sql
INCIDENT-*.md
*OUTAGE*.md
HANDOFF-*.md

# Git worktrees for feature work. Local only, never serve these publicly.
.worktrees/
