# BLKTable

## What this project is
A custom, in-house web app for BLK that borrows Airtable's *concept* (tables of records, add new tables over time) but is our own site and code — not built inside the Airtable product. Each table has two faces: a public shareable form link anyone can submit through (no login), and a private login-gated dashboard where the team reviews and manages submissions.

## Current state
Fully built, deployed, and live.
- **App:** https://blktable.blk.jo (repo `BlkTable/blktable.github.io`, GitHub Pages, custom domain). Static site, single `index.html` + public form pages under `apply/`, `cast/`, `f/`. No build step; uses `@supabase/supabase-js` v2 via CDN.
- **Backend:** **self-hosted Supabase at `https://db.blktable.blk.jo`** (moved off cloud Supabase on 2026-08-05). Photo attachments live in **Cloudflare R2**, served through short-lived signed links minted by an `r2` edge function. The old cloud project still exists, untouched, as a fallback.
- **Local folder:** `C:\Users\ASUS\blktable` (see note in Decisions about folder location).
- **Shipped 2026-08-23:** [#58](https://github.com/BlkTable/blktable.github.io/pull/58), squash-merged as `eb0ba95` and **confirmed live** — duplicate a table, a branch question offering several countries' shops, Country as a column, and the sidebar's left edge. Four database migrations applied and verified (`workspaces/18` the events-link fix, `19` `duplicate_table`, `20` country-from-the-branch-answered, `21` the Lebanon merge). Verified by fetching the served pages and grepping for `side-fold`, `duptbl-modal`, `duplicate_table`, `__country`, `branchListKeys` and `tableSpansCountries` on `/`, `countryNameFor` and `COUNTRY_ROWS` on `/f/`, and by making the public form's new anonymous `countries` read with the live anon key (200, four rows) rather than trusting the merge.
- Older note, kept because the worktrees are still there: `fix/phone-countries-clobber` — its code fix landed on `main` as PR #43, so what is left on the branch is this file's text and some comment wording; and three branches held in their own worktrees by other sessions — `feat/reject-alert-whatsapp` (`C:\Users\ASUS\blktable-alerts`), `feat/universal-form-editing` (`C:\Users\ASUS\blktable-formedit`) and `colour-branch-tints`. **All were cut before the PR #44 merge**, so each needs `git fetch --prune` and a rebase onto the new `main` before opening a PR — #44 edited `renderCustom`, the `.cols-panel` CSS and the toolbar markup, which is where anything touching the records view will collide. `feat/universal-form-editing` is **rebased onto the post-#44 `main`, suite green, and ready for its PR** (see the 2026-08-18 log entry).
- **Work in a worktree, not by switching branches** in `C:\Users\ASUS\blktable`: several sessions share that clone, and `git worktree add -b <branch> ../blktable-<name> origin/main` keeps each one out of the others' way and starts from a fresh `main` (which is what the squash-merge trap requires). PR #44 was built that way.
- **Migration from Airtable: COMPLETE and now EXHAUSTIVE (final sweep 2026-08-10).** Nothing with records is left in Airtable. Of **112 bases / 290 tables** the token can reach, **238 tables hold their rows in BLKTable** and the other **52 are empty in Airtable itself** (0 records — mostly the Iraq bases and unused `Table 2`s); by decision those empty shells are not recreated here. Totals: **158,207 records** — 124,113 form records, 34,036 job applications, 58 casting — with their attachments in Cloudflare R2. **226 tables, 48 archived** at the time of that sweep; **264 tables** now, after the 2026-08-23 merges, the seven empty Airtable defaults, and the duplicate External Training Database. Every table is verified field-for-field identical to its Airtable original — name, type, choice list, order.
  - The 2026-08-08 pass claimed complete but was checked **per base**, and a base counted as done if any row from it had landed. That hid tables skipped *inside* finished bases: the batch generator skipped every base named "job application" or "DON'T USE" (they were the hand-built hiring table's territory), and skipped their secondary tables with them. A per-**table** audit (`coverage_tables.py`) found **28 tables / 30,558 records** still outside — imported on 2026-08-10.
  - **Everything from that sweep is archived** (`config.archived`, `is_active=false`): imported in full and searchable, off the sidebar and Home, and unusable as a public form. That keeps 25 new tables — several called "Job Applicants" — from standing next to the live hiring table. Un-archiving is one flag per table.
- **Backups (from 2026-08-07):** nightly at 02:35 from the `ali` crontab — `pg_dump` custom format, **verified** (`pg_restore --list` must parse it and find the key tables, or the file is kept as `.unverified` and not counted), then **copied to R2**, because a backup on the same server does not survive the server. 14 days kept locally. Proven by restoring into a scratch database and comparing row for row — every table and all 10 logins identical. 438 MB compresses to 19 MB.
- **Every migrated table is verified identical to its Airtable original** — question text, field type, choice list and order — by `audit_fields.py`. All of them currently report zero differences. `align_fields.py` fixes what it finds and `link_photo_fields.py` connects a newly created photo question to pictures already in R2.
- **Field types now cover Airtable's:** multi-select (comma-joined exactly as Airtable stores it), linked records (shows the linked record's own name and opens it), plus the existing text/number/date/photo/phone/email/dropdown/yes-no/DOB.
- **The grid does what Airtable's does:** filter conditions, **group by** any repeating field with counts, a **summary line** (totals and averages for numbers, filled-counts for the rest), **multi-column sort** (shift-click to add a tie-breaker), a column picker, saved views, and **CSV export** with a byte-order mark so Excel opens Arabic correctly.
- **The cards do what Airtable's gallery does (PR #44, live 2026-08-18):** a **Customize cards** panel in the toolbar for the Cards and List views — which file question is the **cover** (or none), whether it is **cropped or fitted**, which **fields** the card carries and **in what order** (the first is the title), and whether their **names** show. Your choice is remembered in your browser; an admin can **Save for everyone** to make it the table's default, exactly as the column picker works. Table-agnostic, so all 226 tables and any made later get it with no wiring — and with nothing chosen a card draws exactly as it did before. Until now these were constants in the page: always the first file question, always the first three answers, always cropped, names always on.
- **A record shows what is worth reading**, not every question: `config.detail_fields` names the handful shown at a glance (Airtable does not publish that choice through its API, so it is set here), and anything else the record answered sits behind a "show every answer" line. Retired questions from older versions of a form only appear on records that actually answered them.
- **Scoring engine (live):** the rules were read out of Airtable's own formulas, not reinvented — 30 scorers + 7 roll-ups for Mystery Shopper, 70 + 2 for QC, six formula shapes in all (equality, checkbox, contains-all, any-of, numeric threshold, roll-up). A database trigger scores every new submission, so it works for public forms, staff edits and imports alike. **Imported rows are never rescored**: older forms scored out of a different number of questions, and recomputing them with today's rule would rewrite history. Validated first — the engine reproduces Airtable exactly on every record of the current form for both tables (1,066/1,066 and 669/669). Percentages display as `94%` with a colour band, and the headline score sits on the card and at the top of the record.
- **Photo thumbnails:** lists, grids and avatars serve a generated 400 px thumbnail (~15 KB) instead of the original (~1.6 MB average, up to 6 MB). A 150-card page went from **238 MB to 2.3 MB**. 102,846 thumbnails cover 98% of the bucket; an hourly sweeper builds them for new uploads, and a photo that has no thumbnail yet falls back to the original automatically. Photo signing is also batched now — one request per page instead of one per photo.
- **Imported answers are real fields.** The importer matches an Airtable question to a field by label, so a renamed or added question used to leave its answer parked in `extra._answers` — present in the record but impossible to filter, sort, column or score. QC had 8 such questions, Mystery Shopper 27, Complaints 26. All are now real fields (0 stranded), and each public form is aligned to today's Airtable form, so staff are not asked dead or duplicate questions: QC asks 171 questions with 89 fields kept as staff-only history, Mystery Shopper 73/65, Complaints 28/20, Praise 11/2.
- **One branch resolver for everything.** `branch_aliases` + `canonical_branch()` know every spelling of every shop, so imports, forms and repairs cannot disagree; adding a newly-seen spelling is one row, not a migration. Complaints and Praise asked for the branch as an ordinary question and so could not be grouped at all — 1,827 rows promoted into the `branch` column. A trigger normalises the column on write, which closes the trailing-space class of bug (100 rows differed from a real shop by one space and showed as a second branch in the sidebar).

- **The app shell now reads like Airtable's** (2026-08-10). Near-black flat theme with exactly two accents — vivid blue for the primary action, pink for sharing and the active-tab underline. Every table carries its own colour + glyph, derived from its key so it needs no schema change. Slim global header with one search box that *is* a **Ctrl-K palette** over every table and saved view, a notification bell badged with how many records are waiting on you, and a circular avatar. Sidebar is Home · Starred · Shared · Workspace with a footer and a pinned blue **Create** button. Each table gets a **base bar** (colour mark, name menu, **Data / Form tabs**, pink Share) — the public form's QR and link moved behind the Form tab and off the records view. Grid gained field-type icons in its headers, a row number that becomes a tick box, an expand button, row height, a `+` field-type picker at the end of the header row, and a live record count. The filter popover gained **condition groups** (`score > 80 AND (branch is A OR branch is B)`), backwards-compatible with saved views written before them. Design spec: `docs/superpowers/specs/2026-08-10-airtable-shell-design.md`, including what was deliberately *not* built (no fake Automations or Interfaces).

What works right now:
- **Home** view (default): all accessible tables as cards/list, ★ star toggle, admin Share button, "N for you" assignment badge + All/Assigned-to-me filter, and an ordering dropdown — **Recently opened** (rows grouped by Today / Past 7 days / Past 30 days / Older, with when you last opened each), Name A→Z, or Starred first.
- **Table builder** (admin): create bilingual EN/AR tables + public forms, drag-reorder fields, field types (text/number/date/yesno/dropdown/phone/photo/email), workflow stages, record actions, review-chain layers, staff-only fields, delete table (math-gated).
- **Workflow engine:** status pipeline (per-stage tabs + move buttons); record actions (WhatsApp/Call/Email templates with `{field}` placeholders); dynamic multi-layer review chain with per-record assignment, delegate-and-return routing, reject-stops; form-manager access; auto-grant-view-on-assign.
- **Review:** inline autosave editing (Airtable-style, admin/editor/manager only), reviewers read-only; Users panel + per-table Share modal. Photo attachments open in a full-screen **lightbox** on click (all tables); **admins/editors/managers can replace** a submission's photo in place; **IP + device are admin-only** (hidden from reviewers and managers in the UI).
- **Permission ladder (every table, built-in or custom): view < edit < manage.** *View* = open the table and read records. *Edit* = change record values, move workflow stages, replace photos. *Manage* = everything an editor can, **plus** approve/reject, send the WhatsApp invite (incl. send-to-all), delete records, and drive the review chain — a manager does not need "can edit" ticked as well. Enforced in the UI (`canEdit`/`canManage`) **and** in the DB (`can_edit_table`/`can_manage_table` in the RLS policies). Admin-only regardless of table flags: creating/editing/deleting tables, the Users panel, QR rotation, and the IP/device fingerprint.
- **Filter (all custom tables + future ones):** Airtable-style condition builder in the toolbar — "Where [field] [operator] [value]", add multiple conditions joined by And/Or, operators adapt to field type (contains/is/is empty, `>`/`<`, is before/after, is any of/none of). Table-agnostic; combines with search, stage tabs, and Assigned-to-me.
- **Searchable dropdowns:** dropdown fields on public custom forms (`/f/`) render as a type-to-search combobox (usable for 300+ option lists like branches / employee names); typing resolves to a matching option so desktop users who type instead of click still submit. Builder's dropdown Options accept one-per-line **or** comma-separated (`English|عربي`), which also fixed the Arabic RTL paste scramble.
- **Built-in tables:** Job Applications (`/apply/`) and BLK Casting (`/cast/`) — hand-coded with their own hiring workflow (New/Approved/Rejected, WhatsApp interview invite). Admins can add extra questions and hide/relabel built-in questions. **Extra questions can be placed anywhere on the form** — each one has a "show after" picker naming the built-in question it should follow (`app_fields.after_field`; empty = end of the form).
  - **Country-aware questions on the job form:** the City list (Jordan-only) shows only for `+962` numbers, and a Yes/No **"Are you Lebanese? / هل أنت لبناني؟"** (`is_lebanese`) shows only for `+961` numbers — required when shown, saved null otherwise; both visible, editable and filterable in review, and both untickable by admins in the built-in form editor.
  - **WhatsApp interview invite is Jordan-only** (`waEligible()` = phone starts `+962`): Send/Resend button, the "send to all unsent (N)" bar, and the schedule popup's wording all respect it. Approving works for every country; non-Jordanian approved applicants must be contacted another way.
- **Form intro (all tables + future ones):** optional bilingual EN/AR intro paragraph stored in `app_tables.config.intro`, edited in the builder, rendered under the title on every public form — `/f/`, `/apply/`, and `/cast/` (built-ins read it through the existing `get_form_config` RPC).
- **Date-of-birth field type (all tables + future ones):** a `dob` field asks for a birth date on the form and reads as a computed **age** everywhere in review (cards, list, detail, with the same 🚩 out-of-range flag as Job Applications), and filters as a number Age — the built-in tables' age behaviour, now available to any table.
- **"Other" choices with a free-text box (all tables + future ones):** any dropdown choice can be flagged as an "other" choice (`|other` in the builder's Options box). Picking it on the public form opens a required free-text box; the answer is stored under `<fieldId>__other` and shown as "Something else — their words" in review, where it's also editable.
- **Conditional questions — "ask this only if" (all tables + future ones):** any question can name another question and the answer that makes it apply (`app_fields.show_if = {field, equals}`, set in the builder). It is hidden on the public form until that answer is given, not required while hidden, and submits no answer if it was never asked — so a form cannot become unsubmittable because a question that does not apply is marked required. In review the row appears and disappears as the answer it waits on changes, and an answer given before the gate closed is dropped on save. A record only hides a question it never answered: an answer already on file stays visible, the same rule retired questions follow. NULL = always asked, so all 226 existing tables are untouched.
- **Custom tables live:** Sample Submissions (vendor-facing product-sample intake + staff evaluation pipeline); Hackathon in the garage (event signup, form `/f/?t=garage-day-7q3m`); **staff document collection** — two identical 3-field forms, Health certificate (`/f/?t=health-certificate-rfil`) and Non-conviction certificate / شهادة عدم محكومية (`/f/?t=non-conviction-certificate-4110`), each = Name dropdown (292 staff) + Branch dropdown (32) + a required photo. HR has view-only access to both; no stages, no chain, no record actions.
  - **Education details / المؤهل العلمي** (2026-08-11, `/f/?t=education-details-79i8`) — Name (the same 292-staff dropdown) + Branch (32) + date of birth (reads as an age) + "Did you go to university?", and behind a Yes: which university, major, and the GPA out of 4. The three university questions are the first users of the conditional-question standard. Nobody has view access yet beyond admins.
- QR codes per form (regenerate = rotate link/token), submission fingerprint (IP + device captured server-side).

- **Table categories (all tables + future ones):** `app_tables.category` groups tables in BOTH the sidebar (collapsible group header with a count, collapsed by default, choice remembered in `localStorage.blk_cat_collapsed`) and Home (cards under category headings). Uncategorised tables stay flat exactly as before. Task tables carry a `Task` badge and hide the public-form QR/link bar.
- **Operate tasks (NEW, in progress):** the 33 active tasks from Operate Solution are imported into the DB as 33 task tables under the **"Operate" category** (249 questions, 31 branches). `kind='task'`, `is_active=false`. Reviewers cannot see them (no `table_access` grants) and the public form engine cannot serve them (anon RLS is restricted to `is_active`), but **admins DO see them** (`is_admin()` returns every row and `loadCustomTables` filters on `builtin_key`, not `is_active`) — which is why the category grouping matters: they show as one collapsed "Operate (33)" group instead of 33 loose sidebar items. No scheduling/trigger engine yet.

- **Self-host cutover — DONE 2026-08-05.** The app now runs entirely on our own server: schema, all config (40 tables / 299 questions / 31 branches), every submission, and the people layer with the review-chain reassignment were loaded and hash-verified against the old cloud project. All 10 logins were recreated (passwords must be changed on first sign-in). Photo attachments come from R2. Verified live end to end after the switch: a public form loads, an anonymous submit succeeds, the photo uploads, the server still captures IP/device, `anon` still cannot read submissions back, an admin signs in and sees every record, and a migrated photo displays.
  - **The one open gap:** 10 job applications arrived while the move was in progress. Their rows are on the new backend, but their photo *files* are still only in the old cloud storage, so those 10 photos won't display until they're copied to R2 (needs a read credential — see Next steps).
  - Rollback if ever needed: revert one commit; the cloud project is untouched and holds the same data.
  - The migration bundle, runbook and verification scripts live **outside this repo** (this repo is public) in a local `blktable-migration/` folder.

- **A record you can add by hand (all tables, 2026-08-13).** Every row in the app used to be born from a public form submit or an import — there was no way to type one in, and the `+` on the grid header adds a *field*, not a row. A blue **＋ New record** now sits in every table's base bar, shown on `can_edit` rather than admin, because adding a row is the same permission as changing an answer already on file. It follows the public form's rules rather than inventing new ones: a question that does not apply carries no answer, an empty answer stores no key at all, and only the questions being asked can be required. Staff-only questions *are* shown, since the person typing is staff. The editor rows are the record panel's own rows, factored into one `edFieldRowHtml()` that both call. The write goes through a SECURITY DEFINER `create_record()` so it can enforce "a table you can edit" — a broad `authenticated` INSERT policy would let any login write into tables they cannot open. **Needs `09-create-record.sql` applied.**
- **One public link per record, not just per table (all tables, 2026-08-13).** A table can declare `config.parent = {table, title, show}` and its form is then served per parent *record* at `/f/?t=<slug>&e=<token>` — the same public page with one extra parameter, one rotatable token per row, and every submission stamped `parent_id`. Anon's view stays narrow: it gets only the fields `show` names, via `get_parent_details()`, never the parent row, and `config_public` carries only the marker `{"scoped": true}`. A scoped form with no token, or a token from another table's record, is "not found" rather than a form that fails on submit. A parent record's panel carries its link, QR, copy and regenerate, for managers only. Built for Events (one signup form, one link per event) and wanted by the Operate tasks per branch. **Needs `10-parent-scoped-form-links.sql` applied** — though the page works either side of it, on purpose.

- **A form that fills up, with backup places behind the real ones (all tables, 2026-08-13).** A child table declares `config.capacity = {slots, backup, open, full}` and the *numbers* live on the parent record, so every event sets its own; `open`/`full` are stage keys from the parent's own `config.stages`, so nothing in the engine is events-specific. The parent row is locked `FOR UPDATE` while the count is read and the row inserted, because a count read outside a lock is a count that *was* true — two people on the last place cannot both take it, and the flip to *filled* happens inside the same lock. `submit_public_form` returns jsonb now so the confirmation says "you have a place" or "you are on the backup list". An **empty capacity number means no limit**, so declaring capacity changes nothing for records carrying no number. Also carries the gate that a link accepts signups only while the parent's status is `open`. **Needs `11-capacity-and-backup.sql` applied.**

- **A question that takes a video, not only a photo (all tables + future ones, 2026-08-13).** A new `media` field type sits beside `photo` and accepts `image/*,video/*`. The existing `photo` type is untouched, so all 226 tables behave exactly as before; this is an addition, not a change. Built for Operate's task checklists, which have **59 "Media" questions** across the 33 tasks, and it also gives the **238 imported applications whose upload was a video** somewhere to be shown — those files have been in R2 since the migration with nothing able to point at them, because a photo field only ever points at something a browser can render.
  - **The type is derived from the key, because nothing else knows.** A stored answer is a bare R2 object key (`<uuid>_<original filename>`) with no MIME type beside it and nowhere to put one without changing the shape of every answer in the app. So image-vs-video is decided from the extension, which survives sanitising. A key with **no** recognisable extension reads as an image — which is what every row written before this existed is.
  - **What a browser will play is narrower than what people upload.** `.mp4/.mov/.webm` play; `.avi/.mkv/.wmv` are still stored and still downloadable, but are offered as a link rather than handed to a player that would show a black rectangle and no explanation.
  - **A video has no thumbnail**, because the hourly sweeper builds them from images. Anywhere a thumbnail would go — card cover, grid cell — it shows a play mark instead. Painting one would have produced an empty grey box that reads as a card that failed to load. Poster frames need ffmpeg on the server and are separate work.
  - **Two ceilings, and they are said before the upload, not after:** 10 MB for a photo, 50 MB for a media question, because a phone photo is 2–4 MB and ten seconds of 1080p is 20–30 MB. Refusing an 80 MB video after it has crawled up shop wifi is the failure this is designed around.
  - **The `r2` edge function is deployed and video upload is live (2026-08-13).** It previously rejected any non-image and capped at 12 MB. Now `video/*` is accepted with its own ceiling (`R2_MAX_VIDEO_MB`, default 50), images keep theirs at 12, and `/health` reports both so the running version can be identified without uploading anything. **Verified against the live function rather than assumed:** a photo still uploads (what all 226 forms use), a non-image/non-video is still refused, a 45 MB video uploads in ~11s without the container restarting, a 60 MB video is refused at 50 and a 13 MB *image* at 12 — so the cap is per kind, not one number. Test objects were deleted from the bucket afterwards.
  - **The deployed function was ahead of our copy of it, and that nearly cost the audit fix.** The 2026-08-12 audit added the staff-profile check to `isStaff()` on the server and it was never brought back into the private repo, so deploying our file would have silently reverted it. The media change was rebased onto the *deployed* source instead, and the repo copy now matches the container byte for byte. **A file edited straight on the server is a file that will be overwritten by the next deploy** — worth remembering for `admin-users`, which has the same shape.
  - **Where an upload is shown, now that there are two panels.** The record panel has a read-only form and an editable one, and only the read-only one listed uploads as a labelled gallery — the editable one (what an admin, editor or manager sees) showed a single image as a header instead. Since a video is never that header, a video was invisible to exactly the people who review them. **Both panels now render the gallery**, the editable one skipping the still image already shown as its header so it is not repeated. Photos: header + gallery. Videos: a play tile that opens full-size in the lightbox with controls.
  - **Two old bugs fell out of building it, both fixed.** Deleting a record only ever deleted the *first* photo field's file, orphaning every other upload in R2 — invisible while every table had exactly one upload question. And the record panel took the first upload question for its Change button but the *last answered* one for the image, so a table with two of them wrote the replacement into a different field than the one on screen.
  - **Known gaps, deliberate:** no poster frames (a video shows a play mark wherever a thumbnail would go — generating them needs ffmpeg on the server), and no way to replace a video from the dashboard, since "Change photo" targets the header and a video is never the header. Re-submitting is the only way to change a video today.
  - 35 new tests (`docs/tests/media-field.test.js`), 238 total.

- **A status that is worked out, not pressed (2026-08-13).** `config.capacity.window = {date, start, end}` names the parent's own questions, and the phase is derived every time it is read: *Done* once the end time passes, *Running* between start and end, *Filled* when every place is gone, *Open* otherwise. Nothing stores it and nothing schedules it — which matters because `pg_cron` is still not installed, and a computed value cannot go stale if a job stops. Signups close when the event **starts**. Absent the window, the previous stored-status behaviour applies untouched, which is what all 226 other tables have. The rule exists twice (JS + `event_time_phase()` in SQL) for the same reason `condMet` does; they were compared case by case and differ only on unparseable input, where the server errs open and the dashboard shows nothing.
- **A `time` field type** — `<input type="time">` on the public form and in every editor. Date and time pickers now open on click in the dashboard too, which the public form already did.
- **`config.child_only`** keeps a table off the sidebar, Home, the Ctrl-K palette and the public-forms list while leaving it a live form — storage read inside its parent. It stays in `customTables`, because everything that looks a table up by id needs it there.

- **Archiving a table, instead of deleting it (2026-08-15).** Until now the only way to finish with a table was **Delete**, which takes every record with it and has no undo — so a table nobody needs any more either stayed in the sidebar forever or was destroyed. **Archive…** is now in the table's ⋯ menu (sidebar and table header, admin only), above the separator with Delete below it.
  - **It is one flag, not a move.** `config.archived` — the same flag the 219 imported Airtable tables have carried since the migration. Nothing is copied, moved, exported or dropped: the row, its fields, its records, its saved views and its access grants are untouched, and the table simply stops being listed. The hiding was already built; what was missing was any way to set the flag from the app.
  - **Where an archived table goes.** Out of the sidebar, Home, the Ctrl-K palette and the public-forms list, and into **Archived** at the foot of the sidebar with a count. Switch that on and archived tables come back into all of those, greyed with an *archived* tag, and open normally with an **Archived** tag in the header — because an archived table reads identically to a live one otherwise, and editing history thinking it is today is the mistake to design against. **Restore from archive** in the same ⋯ menu puts it back.
  - **Archiving deliberately does not close the public form.** The link and any printed QR code keep working. A table is archived by one person on a Tuesday; whether a form should stop accepting answers is what **Restart form…** is for, and it freezes the old table properly. The confirm dialog says all three things out loud — nothing is deleted, it can be undone, the link keeps working — because a person who assumes archiving killed the QR code will not check.
  - **Restoring leaves no trace.** `archived`, `archived_at` and `archived_by` are all removed rather than set to false, so a config round-trips to exactly what it was and a restored table does not sit in the sidebar reading "archived by" for people to hesitate over.
  - 22 new tests (`docs/tests/archive.test.js`), 263 total. The config rebuild is a pure function so it can be tested without a database; the rest read `index.html` as source, including a check that `config.archived` appears raw exactly once — a second hand-written copy is how a table gets hidden from the list but missed by the count.

- **The grid opens first, and you can see that rows are selectable (2026-08-17).** Sort, tick boxes and bulk export were all built, merged and live — and all three were invisible, which is indistinguishable from missing. Three separate causes, none of them the feature itself:
  - **Custom tables opened on Cards.** `customView` defaulted to `"cards"`, so the first screen of every table had no tick boxes (they exist only in the grid), no Hide fields, no Group and no row height. A table in Airtable *is* the grid; it now opens there. The two built-in hiring tables stay on Cards — they are a review queue with a photo per person, not a spreadsheet.
  - **The toolbar was switched on after the empty-case returns.** Sort, Group, Hide fields and CSV all start `display:none` and were revealed at the end of `renderCustom`, below two `return`s for "no records" and "nothing matches". So a brand-new table showed no Sort button until its first record arrived, and any stage tab, filter or search that matched nothing took the whole toolbar away with it — exactly when you most want to clear the filter. The toolbar is now decided before those returns.
  - **Nothing said rows could be ticked.** A row's tick box appears on hover (Airtable's behaviour, and fine once you know), but the header cell where Airtable keeps **select-all** was empty. It now holds a select-all that is always visible, with an indeterminate state when only some rows are ticked. It selects the rows *on screen* — already narrowed by stage, filter and search — because "select all" meaning rows you filtered out is how the wrong records get exported.
- **The payroll export read the wrong rows and always came back empty (2026-08-17).** `payrollHost()` deliberately puts the button on **Events** ("where Faisal is", [#29](https://github.com/BlkTable/blktable.github.io/pull/29)) and `config.child_only` keeps Event signups out of the sidebar, so Events is the only place the button appears — but `openPayroll` read the signups from `currentCustom.subs`, which holds the records of the table *on screen*. Those are events. An event has no `slot`, so `only_slot: 'confirmed'` dropped every one of them and every export, for every date range, said "Nothing in that range."
  - **A second shortfall behind it:** `currentCustom.subs` is capped at `CUSTOM_FIRST` (150) rows, so even from the signups table the sheet would have been short by everyone who had not loaded — and a payroll sheet missing rows looks complete while paying some people nothing. Both sides are now read from the database when the panel opens, paged at 1000 so nothing is silently truncated, with a "Reading the signups…" state so *not loaded yet* cannot be mistaken for *nobody worked*.
  - **`payroll.test.js` could not have caught it.** All 16 tests call `payrollRows` with a signups array they build themselves; nothing tests the caller. Same shape as the video-gallery bug of 2026-08-13 — a test that exercises a function cannot tell you whether anything calls it correctly.

## Decisions made
- **Own code, not Airtable:** user explicitly rejected building inside Airtable; we only mimic the idea.
- **Empty Airtable tables are not recreated (2026-08-10).** 52 of the 290 tables hold zero records — mostly the Iraq bases and unused `Table 2`s. Asked and answered: leave them. Migration means data, not empty shells; anything genuinely needed can be built in the table builder in a minute.
- **Everything from the final sweep lands archived, and what becomes visible is a human call.** A generated table appearing beside the live hiring table is how the data gets confused; archiving first and promoting deliberately is the safe order.
- **Stack:** GitHub Pages (static) + Supabase, same as the Qahwa site. No framework, no build step — keep it simple.
- **Roles:** `admin` (full) and `reviewer` (scoped). Access is per-table via `table_access` with `can_edit` and `can_manage` capability flags, where **manager is a superset of editor** (`can_manage` alone is enough — it implies edit in both the UI and the RLS helpers). No public signup.
- **Anon key gotcha:** must use the legacy anon JWT everywhere; the publishable key format is rejected by Supabase Storage on uploads.
- **Backend future:** decided to leave Supabase Cloud and self-host Supabase (Docker) on Baker's server, with files on Cloudflare R2. Server is up (`db.blktable.blk.jo`); cutover blocked on R2 credentials.
- **Folder location:** repo currently lives at `C:\Users\ASUS\blktable`, NOT under `C:\Users\ASUS\Projects\` as the global rule prefers. Left in place for now to avoid disrupting git/Pages; move is a separate decision.
- Full architecture and the blow-by-blow history live in auto-memory (`project_blktable_app.md`) — richer than this file by design.

## Next steps
**Look at the three panels PR #44 changed by side effect (2026-08-18).** Nothing is known to be wrong — but the tick-box fix it carried is scoped to `.cols-panel`, so **Columns, Group and Color** now draw their checkboxes at 13px on the left instead of stretched across the panel. Worth one glance in the live app, since they were not what was asked for. The new panel itself is under **Customize cards**, next to Filter, in the Cards and List views.

**Rebase the in-flight branches onto `main` before their PRs (2026-08-18).** See Current state — four branches were cut before #44 landed.

**SIX SQL FILES ARE WAITING TO BE APPLIED, IN ORDER (2026-08-13).** All six are in the private folder with a runbook beside them (`workspaces/APPLY-EVENTS.md`), and all the code that needs them is already merged and deployed. **Nothing visible works until they run** — the last of the six creates the Events tables themselves. `09-create-record.sql` (ends in ROLLBACK, change to COMMIT) then `10-parent-scoped-form-links.sql`, `11-capacity-and-backup.sql`, `12-one-signup-per-browser.sql`, `13-swap-and-promote.sql`, `14-events-tables.sql`. Nothing is broken while they wait, deliberately: the public form still sends the two-key submit it always sent, and every page reading a return value the old function does not give falls back to what it did before. Verified on the served file after every merge.

**The events payroll dry run is written and waiting to be run (2026-08-17).** Two SQL files in the private folder — `workspaces/TEST-events-payroll-seed.sql` and `TEST-events-payroll-cleanup.sql`. The seed writes 12 events and 49 signups whose right answer is known in advance (9 people, 44 events, 660 JD at the agreed flat rate of 15) and prints that answer as a query, so the downloaded CSV can be diffed against it row for row. Everything it writes carries `extra->>'_test'`, and the cleanup deletes exactly those rows. Three traps are deliberate: a person who signed up 5 times but worked 3 (the other two are `backup` on the only two events that were full), a name stored `"  ahmad test  "` that must still merge into one person, and a signup with no name that must read as `(no name)` rather than be dropped. Two control events sit outside the pay period to prove the range filter is applied. Names are deliberately fake rather than real staff — fake money against a real person's name left in the production payroll table is the one thing the test must not leave behind. **The payroll fix it depends on is merged and live as of 2026-08-17, so the only thing left is running the seed.** Until it runs the Payroll button will correctly say "Nothing in that range", which is indistinguishable from the bug that was just fixed — so this test is what turns "fixed" into "proven".
- **One thing the seed will answer that could not be checked from here:** no migration file anywhere sets `config.child_only`, though the code reads it and #29 says Event signups should be hidden from the sidebar. Either it was set by hand on the server or the table is still listed. The seed's last query prints which.
- **The SQL files in `blktable-migration/workspaces/` are not in git** — the whole folder is untracked in that repo, which is true of all 17 migrations, not just these two. Worth a decision separately; 17 hand-numbered schema files with no history is a thin place.

**Worth one deliberate test once 11 is in:** two browsers racing for the last place on one event. The row lock is the part no test here can reach, and it is the part that matters most.

**Two follow-ups the media type left behind, neither urgent:**
- **Poster frames for video.** A video has no thumbnail, so cards and grid cells show a play mark where an image would show itself. Needs ffmpeg on the server and a sweeper like the 400 px one that already exists for photos. Worth doing before Operate's 59 media questions go live across 31 branches, because that is when a wall of identical play marks stops being readable.
- **Diff `admin-users/index.ts` against its container copy.** The `r2` function turned out to have been edited straight on the server during the 2026-08-12 audit and never brought back, so our copy was stale and deploying it would have reverted a security fix. `admin-users` has the same shape and has never been checked. One command; the recipe is in auto-memory.

**Storage is worth a number before the Operate task engine is switched on.** 59 media questions across 33 tasks at roughly 2,000 task records a day is a different order of R2 spend than photos have been, and a 50 MB ceiling per answer is the upper bound on how different.

**BLK Events is built on top of these** (project folder `C:\Users\ASUS\Projects\blk-events`, design in its `FIT-TO-BLKTABLE.md`). Two tables and five standards; **all five standards are merged and live** — New record ([#22](https://github.com/BlkTable/blktable.github.io/pull/22)), the per-record link ([#24](https://github.com/BlkTable/blktable.github.io/pull/24)), capacity and backup ([#25](https://github.com/BlkTable/blktable.github.io/pull/25)), one signup per browser ([#26](https://github.com/BlkTable/blktable.github.io/pull/26)), the swap ([#27](https://github.com/BlkTable/blktable.github.io/pull/27)) and the payroll export ([#28](https://github.com/BlkTable/blktable.github.io/pull/28)). **The feature work is finished** — what remains is running the six SQL files.

**Needs one look from you in the browser:** the ⋯ menu on a record card ([#20](https://github.com/BlkTable/blktable.github.io/pull/20), merged and deployed 2026-08-12). Open a card's ⋯ in **card** view, at the top and at the bottom of the list, and check the menu appears with View / Delete and is not cut off. The geometry (`placeRecMenu`) is the one part no test covers, because the harness has no DOM.

**Waiting on a decision from you:** Nobody but admins can see Education details — say if HR (or anyone) should have view-only access the way they do on the two certificate forms, and whether the table keeps that name (renaming does not change the form link).

0. **Switch the Airtable forms off — this is the one with a clock on it (raised 2026-08-10).** Everything is migrated, but the Airtable form links are still live and taking ~90 submissions a day, mostly job applications. Until they are retired or redirected to the BLKTable equivalents, the two systems diverge every day and the cost of catching up grows. Sequence: retire/redirect the links → `recount.py` → top up the delta with the same idempotent importer → cancel the account. Nothing else in this list is time-sensitive in the same way.
1. **Operate tasks — data imported, engine NOT built yet.** The 33 tasks + 249 questions + 31 branches are in the DB (hidden). Remaining work, in order:
   a. **Task instances** — records born from a schedule, not a submit. Columns already added to `app_submissions` (`branch`, `due_at`, `window_end`, `done_at`, `generated`) + a unique index per (table, branch, due slot). Needs the generator function.
   b. **Time trigger** — install `pg_cron` (available, not installed on `cisqemycewkqakyqmusw`) + a generator that runs every ~15 min and creates due records per branch per occurrence. Hourly tasks = every N hours within the window; timezone Asia/Amman. MUST be re-installed on the self-hosted Supabase at cutover.
   c. **Branch-scoped questions** — 100 of the 249 questions apply to exactly ONE branch, 86 to all 31. Stored as `app_fields.options.branches` = array of branch codes (absent = all). The task form must render only the questions valid for that branch. No UI for this yet.
   d. **Thresholds / parameter answers** — NOT in Operate (all 42 numeric questions have no min/max there); this is new. Plan: `app_fields.options.expect = {op, value}` evaluated on submit → pass/fail on the answer.
   e. **Event triggers** — rules table + DB trigger on `app_submissions`; a failed threshold or an incomplete task raises a task/issue elsewhere. (`mark_incomplete_as_issue` is No on all 33 in Operate — they never used it.)
   f. **Embedded view** — public read-only `/e/?v=<token>` page, rotatable token like the QR links.
   Open: dropdown options for the single `Drop-down list` question (task 1051 / qid 626) are not in the export — need a screenshot. Volume estimate ≈ 2,000 task records/day across 31 branches, heavy on photos (59 Media questions) → makes the R2 credentials critical path.
2. **Chain workflow upgrade — APPROACH PROPOSED, awaiting final go, then build.** Three parts (all for chain tables, i.e. tables with `config.layers`; only "Sample Request Form" id `0e7d6487-6785-4325-9ec9-e74cd194968a` has one today, 2 layers):
   a. **Auto 3-stage board (Received → Waiting for feedback → Done)** derived from the chain (NOT manual stage-moving). Store `chain_phase` on `app_submissions`, maintained by the existing `submission_log` trigger. Received (blue) = on the manager's desk to act (new→assign OR returned→close); Waiting (amber) = out with a reviewer; Done (green), rejected = red tag inside Done. Show as tabs + a colored badge per card. Replaces manual pipeline tabs on chain tables only.
   b. **Return-to-assigner sign-off:** when the LAST reviewer submits feedback, route it back to the person who assigned them (the manager) → manager sees a "Mark as done" button → only that click = Done. (Today the last completion silently ends the chain.)
   c. **Realtime auto-refresh:** enable Supabase realtime on `app_submissions` + `submission_log` (publication is currently EMPTY — verified 2026-07-29). Live-update the table list/badges, the open entry's chain, and the Home "N for you" count; add a window-focus refresh as safety net. MUST be re-enabled on the self-hosted Supabase after migration.
   Open question raised with user: whether "Received" folding both new + returned-for-signoff is fine, or they want a separate 4th "Ready to close" stage.
3. **Finish the cutover tail (small).** (a) Everyone changes their password on first sign-in. (b) Once a few days have passed with no surprises, retire the cloud project. *(The in-flight photos are done — see the log.)*
4. **Two questions raised by the first import (both need a decision).**
   - **27 imported Lebanese applications are invisible to the Lebanon reviewer.** His access is scoped by phone prefix `+961`, but 25 of these arrived with no phone number in Airtable at all and 2 have a non-Lebanese number. Options: widen his scope to "came from a Lebanon source", scope on the *Are you Lebanese?* answer instead, or leave it as is.
   - **25 records have no phone.** The column can't be empty, so they read `unknown`. It can be made properly blank instead if you'd rather — that's a small schema change.
5. **Two small calls on the imported applications.** (a) **117 Jordanian numbers have only 7 digits** — incomplete, so not dialable; delete them or leave them searchable? (b) The Lebanon reviewer is scoped by phone prefix; with country now stored as a real field, that scope could switch to the country itself, which is clearer.
6. **Airtable → BLKTable: DONE 2026-08-10** (was "the rest"). Every table with records is out; see Current state and the 2026-08-10 log entry. What is left is a decision, not a migration: **the 29 tables from the final sweep are archived**, and someone has to say which of them deserve to be visible — *Internship Applicants* (1,069), *Junior Python Developer Applicants* (209), *Junior Architect Applicants* (138), *Employee Information Form* (101) and *1 Billion Summit Activation* (71) are live recruitment pipelines, not dead history.
   - **Scope was forms and data only.** Airtable users, collaborators and permissions were explicitly *not* migrated — BLKTable's own logins and per-table access are the access model.
   - Retiring the Airtable account itself is now a business call rather than a technical blocker.
7. **Two admin items outside this repo** — install `pg_cron` on the self-host (needs root; required by the Operate task generator) and rotate the credentials noted in the private project log.
8. **Phase 3:** retrofit Job Applications & Casting onto the generic workflow engine (currently custom tables only) so there's one workflow system.
9. **Extend the searchable dropdown + field-filter to the dashboard side** (offered 2026-07-30, awaiting go): make inline-edit dropdowns and the Filter panel's dropdown value/checklist type-to-search too (same combobox as the public form), so 300+ option fields are usable when editing/filtering, not just when submitting.
10. Optional: whose-turn badge on Home list rows; storage cleanup for deleted-table photos; fully migrate built-in forms to the editable engine.
11. **Shared staff/branch lists (raised 2026-08-04, not offered yet).** Health certificate and Non-conviction certificate each carry their own hand-copied 292-name staff dropdown and 32-branch dropdown. They are byte-identical today, but every new hire means editing N forms and they will drift. Options: (a) a `staff` table + a `staff` field type that reads it live (the `branches` table already exists from the Operate import and could back a `branch` field type the same way); (b) a "duplicate this table" button in the builder so at least cloning is one click. Only worth doing if more document-collection forms are coming.
12. **Open from the country work (2026-08-03), user hasn't decided:** (a) the Lebanon reviewer's scope still filters on phone prefix `+961`, not on the new `is_lebanese` answer — switch it if "Lebanese" should mean the answer, not the number; (b) non-Jordanian approved applicants now have no messaging path at all — if Lebanon hiring grows, they need their own invite text and location (the current invite hardcodes `BLK_LOCATION` in Amman); (c) the Lebanese question only fires on a `+961` number, so a Lebanese person applying with a Jordanian number is never asked.

## Log
- 2026-08-23 (the open question, closed): **The heading on `/apply` and `/cast` is editable, and it needed no database change at all.** [#67](https://github.com/BlkTable/blktable.github.io/pull/67), merged as `d32a0f3` and confirmed live.
  - **The blocker was two unread facts, and both turned out to point the other way.** The 2026-08-19 note said finishing this needed the database, and named exactly what to read — nobody could reach it from that session. Read now: `config_public` is generated by `form_public_config(config)`, a whitelist of `intro`/`color`/`icon`/`parent`/`one_per_device` with **no `title`** — but that column governs `/f/`, the custom forms. **`get_form_config` returns the built-in table's `config` as it is.** So the two built-in forms were never behind the whitelist, and nothing had to be widened. Proved end to end *before* a line was written: a title was set, read back through `get_form_config` **as the `anon` role**, seen to arrive, and rolled back.
  - **Carrying the NAME to the page would have been the wrong fix, and this is the part worth keeping.** The table is called *Job Applications*; the page prints *Job Application · طلب توظيف*. They are different strings on purpose. Making the rename reach the page would have retitled it to something nobody chose, and the dashboard preview would then have shown applicants a heading they never see. So the heading is its own setting — `config.title` — with two boxes whose **placeholders are the heading you would be replacing**, rather than an empty box to guess at. Offered for the two hand-coded forms only: a custom form's public page prints its name, so its name row already *is* its heading and a second box would be two controls for one thing.
  - **What it is built around is the forms that have no title set, which is all of today's traffic.** No title, a `null` title (what two empty boxes actually store) and a whitespace-only title all leave the served heading exactly as it was — confirmed on the live pages after deploy, both still printing what they always printed. The other failure modes: each half falls back on its own so setting only the English cannot blank the Arabic on an Arabic-first form; two empty boxes store `null` rather than `{en:"",ar:""}`, which would read as "set" and blank the page; the markup keeps its own heading so the page is right before the request completes and if the RPC fails; and the text is set with `textContent`, never `innerHTML`, because it comes from the database, is typed by a person, and is printed on a page anyone can open.
  - **The guard fixed earlier the same day earned its keep within hours.** The "nothing writes an unreachable key into a built-in form config" test **failed on `title` appearing** and made the new key be justified rather than waved through. It now asserts the actual rule — every key the editor writes must be read by both public pages — instead of a hard-coded list of two, so it cannot go stale the way the byte-exact version did, and it was re-proved to catch an unreachable key by injecting `theme: "blue"` and watching it fail. A guard that makes you argue for a change is doing exactly what it is for.
  - 18 new tests, **548 across 28 files, all green.**
- 2026-08-23 (last of the day): **`restart_form` now remaps `after_field`** (`workspaces/24`, applied). The gap was real — it repaired `show_if` and every field id in `config` when copying a form, but copied `after_field` verbatim, so the copy's "show this question after that one" still named the **retired** table's question. The public page resolves that anchor by id and, when it cannot, **silently appends the question to the bottom of the form**: an admin who places a question after "Full name" and finds it under "Photo" gets no error to notice, on a form whose printed QR codes already point at it.
  - **It could not actually fire, and that is the more useful half of this entry.** `after_field` is written by exactly one control — `afterFieldSelectHtml()` — which returns `""` unless `builderMode === "builtin"`. So only Job Applications and BLK Casting can carry an anchor, and the value it writes is a built-in **column name**, not a uuid. `restart_form` refuses built-in tables outright. **The one kind of table that can hold an anchor is the one kind that can never be restarted.** Measured rather than assumed: of 4,871 questions exactly **one** carries an `after_field` (`favorite_drink`, on Job Applications, which is correct), and **no form has ever been restarted** — no table carries `config.supersedes`. Damage: none.
    - Worth recording because the first query written to find damage reported "1 broken anchor" and was **wrong**: it assumed `after_field` always holds a uuid and flagged `favorite_drink` as pointing at a field that does not exist. It holds either a uuid *or* a built-in column name, and a check that knows only one of those shapes invents a bug. The fix matches on the exact old uuid for the same reason — so a column name is never rewritten into one.
  - **Why fix an unreachable bug.** Two reasons, both about the next person rather than today. **The safety is a coincidence and an invisible one:** nothing in `restart_form` says "this is safe because anchors are built-in-only" — it is safe because of a rule written in JavaScript in another file. Extending "show after" to custom tables is a natural next feature, the builder already renders the row for it, and the day it lands this starts corrupting anchors with no warning and no test. And **its sibling already did it**: `duplicate_table` remaps `after_field`, `restart_form` did not, and two functions doing the same job disagreeing is a trap for whoever assumes the older one is the careful one. They now remap the same three things.
  - **Built by patching the LIVE function**, dumped with `pg_get_functiondef`, not by editing a copy from the repo — the same discipline the `r2` edge function needed on 2026-08-13, where the deployed copy was ahead and deploying the local file would have reverted a security fix. Diffed against a freshly re-dumped pristine copy afterwards: **the only difference is the one `update`.**
  - **The verification was proved, not trusted.** The bug cannot be reached through the app, so the check builds a throwaway table with one question anchored to another by id, restarts it, and asserts the copy anchors on its own question. Run against the **unfixed** function first, it failed exactly as intended — *"the copy still points at the RETIRED table's question"* — and only then was the fix applied. A verification that has never been seen to fail is not a verification.
- 2026-08-23 (finally): **The suite is fully green — 27 files, ~530 assertions, 0 failures.** [#64](https://github.com/BlkTable/blktable.github.io/pull/64), merged as `1a5b166`. **No application code changed**: `index.html` is byte-identical to `main`. Both long-standing red tests were the test measuring something other than what it claimed.
  - **"a built-in name is never printed as a literal"** scanned the **raw script** and counted a **comment** — the one explaining why the public page prints *Job Application* while the dashboard shows *Job Applications*. A mention in a comment is documentation; the thing worth forbidding is a literal a user can end up reading. Comments are stripped now, quote-aware so a URL inside a string is not mistaken for one, with a guard that fails loudly if the stripper ate real code. The failure names the offending line numbers rather than printing a bare count, and the one surviving literal must actually **be** `BUILTIN_DEFAULT_NAME` rather than merely being the only one left. It also now asserts what makes the whole arrangement safe and was never tested: the pre-load label in the markup **is** repainted from the table row by `paintBuiltinNames`.
  - **"nothing writes an unreachable key into a built-in form config"** pinned the line byte for byte — `tableUpdate.config = { hidden: hidden, intro: serializeIntro() };` — and went red when that line was **correctly** changed to merge onto the existing config instead of replacing it. Replacing wiped `parent`, `capacity`, `scoring`, `alerts`, card layout and column order every time somebody edited a question, silently. **A test that pins a literal line fails the next honest fix to it.** It asserts the rule its own comment describes now: merged never replaced, and exactly the two keys that can reach the public page.
  - **Neither loosening was taken on trust, because a test relaxed until it goes green is worse than a red one.** Each was proven still to catch the bug it exists for by breaking the code three ways and watching it fail: a literal name printed in the script (caught, with line numbers), a `theme` key added to the built-in config (caught, named), and a revert to replacing instead of merging (caught). Then the injected bugs were reverted and `index.html` confirmed identical to `main`.
  - **Why this had been red for days, which is the part worth keeping.** Both tests were *right about the rule and wrong about how to check it* — one measured comments, the other measured a byte-exact line. A failing test that nobody believes is worse than no test: it trains everyone to read "18 tests passed, 2 failed" as normal, and the next genuine failure hides inside the noise. That is the same shape as the 2026-08-17 entry, where three shipped features were invisible because nothing led to them: the code was fine and the signal was broken.
- 2026-08-23 (and the correction): **The 25 dangling links were not broken. The thing next to them was.** [#62](https://github.com/BlkTable/blktable.github.io/pull/62), merged as `6ce9de4` and confirmed live. **No database change** — nothing was repointed, converted or deleted.
  - **The correction first, because the earlier entry in this file was wrong.** They were recorded as failing quietly — "the question renders, the link resolves to nothing, and nobody is told". Reading the code says otherwise: **nothing in the app ever dereferences `links_to_table`.** Grep both pages; that key is metadata the importer wrote and no code reads. A link field's *value* is the linked record's own name, and the row it opens comes from `extra._links`, not from `options`. On top of that, **no record holds a value for any of the 25** — not one key, not even blank — so the question is never rendered on a record at all (a question a record never answered is skipped) and no dead link is ever put in front of anyone.
  - **The measurement that settled it:** of 175 link fields whose target table exists, **126 (72%) hold answers**; of the 25 whose target is missing, **0 do**. Link fields are normally used. These 25 never were, because there was never anything to link to.
  - **So the data was left alone, deliberately.** Deleting the 25 questions would have broken the documented "every migrated table is field-for-field identical to its Airtable original" invariant that `audit_fields.py` checks, for no user-visible gain. Repointing them would have invented a relationship nobody has ever used. Neither is a fix; both are motion.
  - **What was actually broken, and it is worse than the thing reported.** **Two different questions share the type `link`:** a link **button** the builder makes (`{url, text, text_ar}` — three of them) and a linked **record** the importer made (`{links_to_name, links_to_table}` — **200 of them across 103 tables**). The editor only ever knew about the first. `optsToString` returned `""` for a linked record — its options object has neither `.list` nor `.url` and `.length` is `undefined` — so the Options box came up empty; the save's `link` branch then read that empty box, failed the `http://` check and **`return`ed, abandoning the save of the entire table**, reporting *"A link field needs a URL starting with http:// or https://"* about a question that has nothing to do with URLs.
    - **An admin could not save the form editor on any of those 103 tables at all** — not the field, the whole table. And had anyone got past it by typing a URL to make the message go away, the linked-record metadata would have been overwritten and that field's "shows the linked record's name and opens it" behaviour destroyed.
    - **Why nobody had hit it:** all 103 are imported history in `OLD (Airtable)`, which is not where anyone edits a form. It was waiting for the first person to tidy one.
  - **The fix.** The metadata rides on the builder row and goes back exactly as it came — nothing in that editor can meaningfully change which table a link points at — and the Options box is read-only, because the text in it (`Linked records → Baristas`) is a description rather than a value anyone can retype. It is read **only while the question is still a link**, so switching the type to Short text does not leave `links_to_table` behind on a field that is no longer one.
  - **A side effect worth having:** opening one of those 12 tables now *shows* what a dangling link points at, by name, instead of an empty box. That is as visible as the 25 can honestly be made without rewriting history.
  - 12 new tests, 528 across 27 files. Three of them read the save as source, because **the order is the fix**: the linked-record check has to run before the URL rule or it still rejects the save first. One tests a half-written linked record that has lost its name — it must still count as a linked record rather than falling through to the URL branch, which is the same bug in miniature.
  - **The general lesson, which has now cost twice in one day:** `record_share_token` reported a missing function as a missing permission, and this reported a missing URL for a question that never had one. Both times the message named a cause the code had guessed at. A message that describes what the code *expected* rather than what actually happened sends the reader to the wrong subsystem, and they stay there.
- 2026-08-23 (last): **`External Training Database` no longer exists twice** (`workspaces/23`). 265 tables → **264**. One table, its 4 records, no orphans.
  - **It was not a merge, and calling it one would have been wrong.** Both copies held the same four training reviews — normalised to `label=value`, record 1 matched record 1, 2 matched 2, 3 matched 3, 4 matched 4, same answer counts, same dates (2024-07-13 / 07-25 / 07-28 ×2). Moving rows across would have produced eight records of which four were duplicates of the other four: worse than either table on its own. So the operation was "remove the second copy and make sure nothing pointed at it", and the migration says so rather than calling itself a merge.
  - **Which copy went:** the one whose own base is named *(Training Tracker) Old* — the business's own label for which is superseded. Its slug was the machine-generated `barista-evaluation-training-tracker-old-extern`; the keeper's is the hand-made `ba-external-training`, and the keeper has `Total Score` promoted to position 4 rather than buried at 14, which is somebody having tidied it.
  - **The part that nearly went wrong, and it was not in the row counts.** Two linked-record questions pointed AT the copy — both called `Level 6 Quiz (SWAT) copy`, both on `Baristas` in that same old base, one of them holding 4 answers. Deleting the table without touching them would have left two questions pointing at nothing. **It would not have broken loudly**: the database already carries **25** link fields pointing at tables that no longer exist, so this would simply have become 27 and nobody would have noticed — though see the correction above: those 25 turn out to be inert, because nothing in the app reads `links_to_table` and no record holds a value for any of them. They were repointed at the keeper *before* the delete, so there is no moment at which they name a table that is gone — safe precisely because the two tables' contents are identical, so the link resolves to the same four records either way. Verified afterwards: still exactly 25 dangling links, not 27.
  - **What this deliberately does NOT settle.** External Training Database is one of several tables duplicated between `Barista Evaluation` and `Barista Evaluation (Training Tracker) Old`, and it is **the only one that is genuinely identical**. The others diverged: `Training List` 348 rows against 199 sharing 187, `Level 5 Quiz` 202 against 106 sharing 82, `Level 0 Quiz` 108 against 78 sharing **7**. Two near-copies of a base that were both used, not one copied once. The rest belong to the per-process merge in `merge-tables/00-RUNBOOK.md` and must not be treated the same way.
  - Backed up to `23-BACKUP-external-training-database-copy.json` (private folder) — the removed table row, its 27 questions, its 4 records and the two link fields exactly as they were before the repoint — then dry-run with `commit` replaced by `rollback`, then applied.
- 2026-08-23 (later still): **Seven untouched Airtable default tables deleted** (`workspaces/22`). 272 tables → **265**, 21 blank rows gone, no orphans anywhere. When you add a table in Airtable it arrives pre-made — questions called Name / Notes / Assignee / Status (sometimes Attachments / Attachment Summary) and three blank rows — and seven of those were created, never touched, and came across in the migration exactly as they were: `Contractor log`, `DON'T USE BLK - Receiving Form (Purchasing)` (the three-row one, not the 15,795-row one), `Employee Information Form` [Job Application - 2025 August], `New Cooking Class form`, `Stock Transfer Form (Transfers)`, `Untitled Base`, `Water Filter Expense`.
  - **"Empty" was read out, not assumed.** Every answer on all 21 rows was listed. Six keys are non-empty and all six are the same thing — Airtable's own error object from a formula with nothing to compute from, `{"state":"error","errorType":"emptyDependency","value":null,"isStale":false}`. That is a serialised error, not an answer. Nothing else at all: no status, no branch, no assignee, no upload, and **zero rows in `submission_log`**, so no review history to lose and no file in R2 to orphan.
  - **Delete rather than Archive, and the distinction is the point.** Archive is the ordinary ending for a table somebody finished with and it is the right default everywhere else, because it keeps every record. These are not that — nobody ever used them, so there is nothing to keep, and archiving would have moved seven empty shells from one list into another.
  - **The guards are the real content of the file.** Every claim above is re-checked against the live database inside the transaction and the whole thing aborts if any is wrong: exactly 7 tables and 21 records, **not one answer** that is not the emptyDependency error, no status/branch/assignee/parent/share link, no `submission_log` rows, no `table_access` grants, no table declaring one of them as its parent, and no upload in any photo/media/file question. "I looked and they were empty" is a statement about five minutes ago, and this is the one operation with no undo.
  - **Backed up first anyway:** `22-BACKUP-empty-leftover-tables.json` in the private migration folder holds all 7 table rows, all 39 questions and all 21 records exactly as they were, taken immediately before the delete and validated as parseable JSON. Then dry-run — the same file with `commit` replaced by `rollback` — and only committed once the guards and the post-delete verification had both been seen to pass.
  - **Not deleted, and still open:** `External Training Database` exists twice with byte-identical content (4 rows). That one holds real data, just twice over, so it is a merge decision rather than a leftover and was left alone.
- 2026-08-23 (later): **The old Lebanese job applications are merged and live** (`workspaces/21`). `AUB Job Applicants` existed twice — 20 rows in *Lebanon - Job Application - 2025 (FULL)*, 15 in *(NEW)*, identical 30-question set and **zero overlapping rows**, so 35 distinct applicants rather than one list imported twice. They now sit in **`Barista Application (Lebanon)`** in Main under the Lebanon category, which is the same form rebuilt. 605 answers carried across, none lost, records renumbered 1–35.
  - **Records were MOVED, not copied** (`update table_id`), so their ids survive and with them `submission_log`, the review chain and anything else that names a record. Copy-then-delete would have renamed 35 people.
  - **Four questions were reworded when the form was rebuilt and had to be aliased by hand:** *Why do you want to join?* → *Why do you want to join the team?*, *Previous work experience? - شو اشتغلت من قبل؟* → *Previous work experience?*, *Name a faculty member that would vouch for you* → *Name an AUB faculty member that would say good things about you*, and *A short video* → *Short video*. Matching on the label alone would have filed 35 real answers in staff-only duplicates of questions the form already asks. A verification in the file fails the whole migration unless all 35 records answer the reworded join question on the **live** field.
  - **Two questions share a label on both forms and mean different things, and that is the judgement call in the file.** *Education Level* (AUB: Freshman/Sophomore/Junior/Senior/Graduate student — the new form: Highschool Graduate / Currently in College / College Graduate / Masters) and *How did you find about the work opportunity?* (AUB: Through a friend / Through AUB Faculty / On AUB whatsapp Group — the new form: Online - Instagram / Word of Mouth). All 35 Education Level answers and 25 of 35 "how did you find" answers are outside the new form's choices.
    - **Moving them onto the live question would have destroyed them by being read.** A record holding a value its own dropdown does not offer shows an empty select, and saving that record writes the empty back. Not a display problem — a data-loss problem triggered by opening the record.
    - Widening the live form's choice lists was the alternative and was rejected: it changes what every future Lebanese applicant is asked, which is a product decision, not something a data merge gets to make. So both came across as staff-only history (`Education Level (AUB 2025)`, `How did you find about the work opportunity? (AUB 2025)`), the same treatment QC's 89 and Mystery Shopper's 65 fields got. The live questions stay empty for these 35, which is honest: they were never asked them.
  - **`Full Time ` — with a trailing space — is why every answer is trimmed on the way in.** 12 records answered *I'm looking for Full/Part time?* with ten characters against a choice list's nine. It reads correctly everywhere and matches nothing, so those 12 would have been unfilterable and would have blanked on save for exactly the reason above. This is the trailing-space class the branch resolver already exists to stop, showing up in an *answer* rather than a shop name.
  - **Six questions were dropped because nobody ever answered them** in either table (Status After Interview, Shop Leader Notes and the four scoring questions). A staff-only column nobody filled in is clutter, not history. Nine were created, so the form went from 20 questions to 29.
  - **The `country` column is stamped `lebanon`** on all 35 — the derived metadata column, not an invented answer to the form's own Country question, which nobody answered and which stays empty.
  - **Nothing was deleted.** The two AUB tables stay archived in OLD, now empty, each carrying `config.merged_into`. Reversing this is moving 35 rows back. `1 Billion Summit Activation` (71 rows, event staffing) and `Marketing Position` (23) are different roles, so they moved up to Main beside the merged form as their own tables rather than being folded in — metadata only, one flag back.
  - **Run as a dry run first** — the same file with `commit` replaced by `rollback` — so every count and every verification was seen to pass before anything was written.
- 2026-08-23: **Four things asked for together, and three of them were the same shape** — something built for one country, one list or one group that had to handle several. Branch `feat/multi-country-groups`, [#58](https://github.com/BlkTable/blktable.github.io/pull/58). **The three database migrations are already applied and verified** (`workspaces/18`, `19`, `20`), so the live bug below is fixed whether or not the branch has merged.
  - **"You cannot share records of this table" was not a permission, and the sentence was a guess.** Every event created since the migration had no shareable link. `record_share_token()` mints its token with pgcrypto's `gen_random_bytes` and is declared `search_path=public`; pgcrypto lives in `extensions`, so the call could not resolve and the function threw *function does not exist*. The page reported **any** error from that RPC as the permission refusal, so a missing function read as a missing permission — to an admin, who had the permission.
    - **The evidence that it had never worked, rather than stopped working:** exactly one submission in the whole database carries a share token, the one minted while the migration ran as `postgres` with a wider search path. That is the "it worked when we first did it".
    - **Checked as a class, not as one bug.** Two public functions call an extensions-schema function unqualified — this one and `rotate_form_token`, whose `gen_random_uuid()` also exists in `pg_catalog` and so resolves anyway. This was the only one that could fail, and it is fixed by naming the schema rather than by widening the path, because a widened `search_path` on a SECURITY DEFINER function is its own problem.
    - All 14 events were confirmed to mint their own link afterwards, one per record, in a transaction that was then rolled back so the real tokens are still minted on first use.
    - **The lesson worth keeping: a catch-all error message is a diagnosis, and it will be wrong.** Reporting "you cannot share records of this table" for every failure pointed at the wrong subsystem entirely. The page now repeats the database's own words for anything that is not the refusal the database actually raises.
  - **Duplicate a table**, in both the sidebar and header menus, admin only. Distinct from **Restart form…** two lines below it, which retires the original and hands the copy its public link; this leaves the original untouched. One call (`duplicate_table`), because a table is a row, its questions, the ids those questions are referenced by, its access grants and maybe its records, and as five requests a failure after the second leaves half a table in the sidebar. `config.parent` is deliberately not copied — the app finds a scoped form with "the first table whose parent is me", so a second child of one parent is how a record's link starts opening the wrong form, which is the bug in the entry above. The migration verifies itself: it duplicates a real table with records, checks the copy question for question and record for record, checks no answer still points at a question id that is not in the copy, and refuses to commit otherwise. **It also found that `restart_form` does not remap `after_field`** — fixed later the same day in `workspaces/24`; see that entry, including why it could never actually have fired.
  - **A branch question may now offer more than one country's shops** (`options.list = "jo, lebanon"`), so a form covering both is one form with one link instead of two of everything. The country of a record is then read off the branch that was **answered** rather than off the question, which is the only thing that can still say when the question offers several. For a single-list question the two rules give the same answer, and the migration refuses to commit unless the new rule agrees with every row already stamped — it did, so all 226 existing tables are untouched. No backfill, on purpose: a form that only ever offered one country cannot have collected a record from another. The public form puts the country in each option's **text** rather than in a heading, because that control is a type-to-search box and a heading is invisible the moment anyone types.
  - **Country is a column now, not only a count.** The database has stamped `app_submissions.country` since country scoping went in and the sidebar has listed the totals, but there was no column — so a table spanning two countries could be *counted* by country and not *read* by it. `__country` joins `__branch` and `__created` in sort, group, filter, cards and CSV. It is an always-on grid column only where the answer can differ, because a Country column reading "Jordan" on all 1,066 rows of a Jordan-only form costs width and says nothing.
  - **A group of one is not a group.** The migration made one category per Airtable base and **37 of them hold a single table**, so the sidebar showed a fold arrow, the base's name and the count "1", opening onto one table usually called the same thing — two rows and two clicks for one form, and no colour mark to recognise it by, which is what "a lot of tables with no emblem, inside a group for no reason" was describing. Those tables are listed directly now. Nothing is written to the database: the category is still on the row, saying where the table came from, and it groups again by itself the moment a second table joins it. The fold arrow also moved out of the label and into the same 20px slot a table's colour mark occupies, so every row in the sidebar starts at the same x — checked by a test that compares `.side-fold`'s width against `.tmark`'s in the stylesheet, so the two cannot drift.
  - **The duplicate-table audit found no duplicate tables, and that is the finding.** 272 tables produce **32 name-collision groups**, and going by name and row count alone at least eight of them look like obvious merges. Comparing actual row *content* — normalised to `label=value`, because every imported copy gave its questions new ids so the raw jsonb can never match across tables — says otherwise:
    - **`BLK - Handover Sheet` exists four times with exactly 1,950 rows each and shares only 72 rows between any two of them.** Four periods of the same sheet, not four copies. Same story for `Employees` (60 rows each, 4–27 shared) and `BLK - Handover Sheet 2022-23` (3,612 vs 3,559, **13** shared). Merging those on the strength of the name and the matching count would have fused ~7,800 distinct rows into one table and destroyed the periods.
    - The eleven `New Store Opening` tables and the five `Shop Management` ones share a question set because they are **one checklist instantiated per store**, and they share rows because the checklist items are the same text. High overlap is what a template looks like, not what a duplicate looks like.
    - `Product Feedback (Chai Karak)` / `(Karkadeh)` / `(Salted Honey Latte)` are three products. `Wastage (Branches/Date)` / `(Items)`, `Task Management (New)` / `(To-dos)`, `Talabat Invoices` / `(clean)` and `Assets` / `(older list)` are all pairs that differ. `Branches`, `Projects`, `Tasks` and `Employees` collide across unrelated bases.
    - What **is** redundant: `External Training Database` twice with identical content (4 rows), and about seven **empty Airtable leftovers** — tables of three blank rows with one distinct value between them (`Contractor log`, `New Cooking Class form`, `Water Filter Expense`, `Stock Transfer Form (Transfers)`, `Untitled Base` and two others sharing their exact fingerprint). Those are deletions, not merges.
    - The real version of this job is the one already scoped in `merge-tables/00-RUNBOOK.md` and never run: Airtable kept **one base per year and per country**, so ~177 tables collapse to ~85 processes. That is a decision per process about which histories to fuse, not something a name match can drive. **The name-and-count heuristic is the trap, and it agrees with the truth on none of the four biggest groups.**
  - **The Lebanese job applications are one decision away.** The old ones are `AUB Job Applicants`, which exists **twice** — in `Lebanon - Job Application - 2025 (FULL)` (20 rows) and `(NEW)` (15) — with an identical 30-question set and **zero overlapping rows**, so those two are a clean 35-applicant merge. Where they go is the open question: `Barista Application (Lebanon)` and `Content Creator Application (Lebanon)` are the new empty Lebanon forms in Main and they share **13 of AUB's 30 questions each**, so the mapping is real but not automatic, and 17 AUB questions (the video, the interview scoring, the shop-leader notes) have no home in either and would have to land as staff-only history the way the QC and Mystery Shopper imports did. `1 Billion Summit Activation` (71 rows, event staffing) and `Marketing Position` (23) are different roles again and belong beside them as their own tables rather than folded in. Nothing has been moved.
- 2026-08-23: **A shop in Lebanon could not submit the QC form at all, and every retry made it worse.** Reported as "Couldn't submit — please try again" under a form full of ticked, uploaded photos. `Shop Spot Check (QC)` asks **52 photo questions**, and both pages fired every chosen photo at `/functions/v1/r2/upload` **at once**, then failed the whole submission on the first refusal. Kong throttles that route per IP (30/min, 400/hour, `policy: local` across 2 workers, so ~60/min in practice) and a shop is one office IP, so ~20 photos in and the rest came back **429**. `Promise.all` threw the submission away, pressing Submit again re-uploaded all 52 into the bucket that had just overflowed, and it could never recover: Kong's log for 23 Aug shows **1,422 uploads, 262 refusals and 33 timeouts** from two Lebanese IPs across 11:50–12:12, and **not one submission landed**. The photos that did upload are orphans in R2 attached to no record.
  - **The sizing comment in `kong.yml` names the wrong assumption out loud:** "every submission carries at most one photo". That was true of the hiring forms it was written for on 2026-08-16 and false of the QC form that already existed. The limit is not wrong, the page's behaviour against it was.
  - **The fix is a queue, in both pages** (`f/index.html` and `index.html`, the way `condMet` and the size ceilings live in both): **3 lanes** instead of all at once, **4 tries** per photo backing off on the server's own `RateLimit-Reset` (capped at 65s: a minute bucket is worth waiting out, an hour bucket is not), and a **`WeakMap` of the upload in flight keyed by the chosen `File`**, so a second press of Submit costs only the photos that have not gone up yet. That last one is what breaks the storm; lanes and retries alone only slow it down. A 400 or 413 is never retried, the button counts "Uploading photo 12 of 41…" so a slow submission does not read as a frozen one, and a refusal that survives four tries says *wait a minute and press Submit again, the photos that already uploaded are not sent twice* rather than "try again".
  - Same queue on **Add a record by hand** in the app, which had the identical `Promise.all` over every chosen file.
  - **41 checks in two new files.** `docs/tests/upload-queue.test.js` (23) pulls the queue out of **both** pages and fails if they drift: the lane ceiling, the retry classes, `Retry-After` beating `RateLimit-Reset`, an hour-long reset capped rather than obeyed, answers coming back in question order and not completion order, and the one that matters — a second run uploading only what the first did not manage. `docs/tests/upload-queue.chrome.js` (18 across two runs, skipped without Chrome) presses Submit on the **real page** with 8 real `File`s and a stubbed Kong that refuses the first 6: never more than 3 in flight, 8 photos + 6 refusals = 14 uploads and no photo sent twice over, the submission lands, and the never-clears run leaves the wait message instead of a false success.
  - **Left alone deliberately:** the Kong numbers. With the queue in place one submission fits, so raising an anonymous upload limit is headroom rather than the fix. If it is wanted, `functions-v1-r2-upload` at **150/min, 1500/hour** sizes for two people filling the 52-photo form in the same minute. Also worth doing later: shrinking photos in the browser before upload, which is what would kill the 33 timeouts.
  - **Found while testing, not caused by it:** `docs/tests/card-panel.chrome.js` fails on a pristine `origin/main` (`93f4195`) and passes at `889da2a`, so something merged between the two broke it. Its page throws before writing results; `cardReset` missing from the harness's grab list is part of it but not all.
- 2026-08-19: **One form editor, for every form, editing the whole form.** Two complaints, one root. Editing a form's questions worked on tables and nowhere else — Job Applications and BLK Casting showed a paragraph saying their questions were "hand-coded in the app" and could not be previewed, let alone changed. And even on a table the editor stopped at the questions: the name was read-only with a note pointing at Table settings, and the two built-in forms could not be renamed at all, because the builder hid the name row whenever the form was built in.
  - **The fix is a description, not a special case.** The two hand-coded forms are now in `BUILTIN_SCHEMA` the way a table describes itself — page order, label, Arabic label, type, choices, and the prose condition on the two questions the page asks conditionally. Described that way they preview through `renderFormPreview` like any other form. `formFace(scope)` is the shared description; everything on the Form tab asks it instead of asking which view it is in.
  - **The editor is one node with two homes.** `#bld-body` is *moved* between the builder modal (creating a table) and the Form tab (editing one) rather than copied, so `bld-name`, `bld-fields` and `bld-stages` mean the same thing in both places and saving goes through the same code. That is what makes "edit everything" true in both: name, intro, questions, and for a table its stages, actions and review chain. Every "edit this form" in the app now goes through `openFormEditor()`.
  - **Renaming a built-in form lands everywhere in the dashboard**, because the name is read from its `app_tables` row via `builtinName()` instead of printed as a literal in a dozen places. **It does not reach the public page, and that half was deliberately cut before shipping.** `apply/` prints `Job Application · طلب توظيف`, which is not the table's name, and the only channel to it is `get_form_config`. An editable "Heading on the public form" was built, tested and then removed on the evidence of PR #47: `config_public` is a **whitelisted Postgres GENERATED column** — `event-signup` exposes only `{parent: {scoped}, one_per_device}` out of a much larger config, and the `parent` value is rewritten rather than passed through — so a `config.title` invented by the app would never reach `apply/`. A control that writes a key nothing reads looks like it worked and does nothing, which is worse than not offering it. See Next steps.
  - **Three quieter bugs the same change exposed.** `apply/` and `cast/` could only resolve an anchor for the *removable* built-in questions, while the dashboard offers all of them under "show after" — an unresolvable anchor drops the question to the bottom of the form with no error anywhere. A built-in form could not save with zero extra questions, which is BLK Casting's situation today. And a task table's Form tab was taken away entirely, which left its fields uneditable once Table settings folded into that tab; the tab stays now with only the QR half hidden.
  - **Another session's uncommitted work was picked up and handed back.** `C:\Users\ASUS\blktable` was on a clean tree when this started, so the branch was cut there; a second session then wrote its alert/notify feature into the same `index.html` while this one was editing it, and the copy taken into the worktree carried both. 53 hunks, 9 theirs: split with a script, reverse-applied out of this branch, and applied back into the shared clone as the uncommitted changes they were. **The worktree rule in Current state exists for exactly this and was followed too late** — cut the worktree first, before the first edit, not after.
  - Verified with the suite (16 files, all green, including the 19 new tests in `docs/tests/universal-form-editor.test.js`) and by rendering the Job Applications and BLK Casting Form tabs — preview and editor — in headless Chrome against a stubbed database, because the suite still cannot open a screen and say what is on it.
- 2026-08-17: **Three features that were built, merged, deployed — and invisible.** Asked for sort, tick boxes and bulk export, "we agreed it is an Airtable replica". All three already existed and were live; the live page was checked for their markup before a line was written, and every marker was there. What was missing was any way to *arrive* at them: the grid was not the view a table opened in, the toolbar was switched on after the two early returns that handle "nothing to show", and the header cell Airtable puts select-all in was empty. A feature nobody can find is not shipped, and the bug report "there is no sort" was accurate about the product even though the code was fine.
  - **The lesson worth keeping:** all three defects live in *reveal* code — a default, an ordering, an empty cell — and none of them is reachable by a test that calls a function. The suite has 263 tests and not one of them opens a table and asks what is on screen.
  - Also fixed the payroll export, which read the records of the table on screen rather than the signups, and so returned nothing for every range. See Current state for why the test suite could not catch that one either.
  - **Merged as [#35](https://github.com/BlkTable/blktable.github.io/pull/35) and confirmed live**, by fetching the served page and grepping for `grid-selall`, `rchk-all`, `fetchAllRows` and the Grid-active toggle rather than trusting the merge. The same pull brought in [#34](https://github.com/BlkTable/blktable.github.io/pull/34) (CDN scripts pinned with SRI, single quote escaped in `esc()`), which had landed separately and is also live.
  - **Shipped WITHOUT the test suite being run, and that is a real gap, not a formality.** There is no Node on this machine — `node` is not on PATH, not in Program Files or LOCALAPPDATA, and not within four levels of `C:\`; the `python` on PATH is the Microsoft Store stub. What was checked instead: bracket balance against `HEAD` (identical deltas), and that no test asserts on any markup touched. That is not a green suite. **`docs/tests/` still needs a run somewhere that has Node**, and it is the first thing to do if the grid misbehaves.
  - **A second session was editing `index.html` at the same time**, building a form-is-live toggle. Its work was classified hunk by hunk and deliberately left out: 14 hunks in the diff, 10 mine, staged by patch rather than by file. Their changes were stashed across the merge and popped back cleanly, and remain uncommitted for them to finish. This is the 2026-08-13 trap — a feature going out inside someone else's commit — caught by reading the diff instead of assuming a clean tree was still clean.
- 2026-08-15: **Archive, so a finished table is not a deleted one.** Delete was the only ending a table had, which meant a table nobody needs was either kept in the sidebar forever or destroyed with its records. **Archive…** now sits in both ⋯ menus above the Delete separator, and **Restore from archive** undoes it.
  - **Nothing was built to hide the table — that already existed.** `config.archived` and the sidebar's *Archived* toggle have been there since the Airtable migration, hiding 219 imported tables. The only thing missing was a way to set the flag from the app, so this is a menu item, a confirm, one `update` of `config` and the two tags that say which state you are looking at. Whether the flag was set by the migration script or by a person is a distinction the app deliberately does not make.
  - **The one deliberate non-behaviour: it does not close the public form.** Tempting to fold in, and wrong — freezing a form is `restart_form`'s job and it does it in one transaction with the slug handover, and the RPC argument trap says a form path is the last thing to touch casually. So the confirm says the link and the printed QR keep working, rather than the code quietly assuming nobody will notice.
  - **An archived table opens and reads exactly like a live one**, which is the trap. Greyed with an *archived* tag in the sidebar, an **Archived** tag in the header, and the table reopens after archiving or restoring so those tags cannot disagree with the menu you just used.
  - 22 tests, 263 total, whole suite green. Four of them read `index.html` as source: both menus must confirm before archiving, neither may confirm before restoring, the write is gated on `isAdmin`, and `config.archived` may appear raw exactly once.
- 2026-08-13: **A question that takes a video — and it went out under someone else's commit message.**
  - Asked for "a media type of question so people can upload videos". Built as a standard for every table rather than for the form that prompted it: a `media` type beside `photo`, accepting `image/*,video/*`, with `photo` left exactly as it was so no existing table changes.
  - **The interesting constraint was that nothing stores a MIME type.** An answer is a bare R2 key, so every consumer — card cover, grid cell, gallery, lightbox — has to work out image-or-video from that string. Deriving it from the extension keeps the answer shape untouched; the rule that mattered most was making an *unrecognised* key read as an image, because that is what every row written before today is.
  - **Three failure modes the design is built around, none of them the happy path:** an `<img>` pointed at a video is a broken-image icon; a video has no thumbnail to paint, so a cover would be an empty grey box that reads as a broken card; and `.avi`/`.mkv` upload fine but play nowhere, so they are offered as a download rather than a player showing black.
  - **The blocker was the `r2` edge function, and it is not in this repo.** It rejected any non-image and capped at 12 MB, so the first thing tried on the live form was "Videos can't be uploaded on this form yet" — the page doing exactly what it was built to do, which is refuse in words rather than fail silently. Now deployed: video allowed, its own 50 MB ceiling, `/health` reporting it.
  - **The two things called unverifiable turned out to be fine, and are now measured rather than assumed.** A real 45 MB video uploads in ~11s with the container never restarting (so the buffered ~2x memory fits), and Kong's body limit is not in the way. A 60 MB video is refused at 50 and a 13 MB image at 12, so the ceiling follows the kind of file rather than one number for both.
  - **The deployed function was AHEAD of our copy of it.** The 2026-08-12 audit's staff-profile check had been made directly on the server and never brought back, so deploying our file would have quietly undone a security fix from the day before. Caught by diffing the container's copy against the repo's *before* writing anything, which is the only reason it was caught at all. The change was rebased onto the deployed source and the two now match byte for byte. Backing up the live file before overwriting it is cheap; assuming your copy is current is not.
  - **Two pre-existing bugs surfaced by adding a second upload type:** deleting a record deleted only the first photo field's file and orphaned the rest in R2, and the record panel's Change button named the first upload question while the image came from the last answered one — so with two of them it wrote into a field other than the one on screen. Both invisible while every table had exactly one upload question. Both fixed.
  - **Then the video uploaded and could not be played, and that one was mine.** The record panel has two forms and only the read-only one rendered the gallery; the editable one — what an admin, editor or manager gets — showed a single image header instead, and a video is never that header. So the upload worked, the card showed its play mark, and the record it opened had nowhere for the video to be. Both panels render the gallery now. **Why it was not caught:** the upload path was verified end to end against the live server, but the display path only through tests, which called `photoSectionHtml` directly and so never noticed that one of its two callers did not exist. A test that exercises a function cannot tell you whether anything calls it.
  - **And the fix's own test caught the fix.** Skipping the header photo by field id alone was safe only because of how the header is chosen a few hundred lines away; if that ever changed, the failure would be a video nobody can play — this bug again. The skip now applies only to still images, so a video can never be skipped whatever it is handed. 241 tests.
  - **How it shipped is the part worth remembering.** Another session was working in the same tree; its commit staged everything, so this whole feature — `index.html`, `f/index.html`, `media-field.test.js` and its README row — went out inside **#32, "An event's status is worked out, not pressed"**, a commit about something else entirely. Confirmed live on both served pages afterwards. Nothing is wrong with the code, but the history now hides a feature inside an unrelated message, and `git log -- f/index.html` gives a misleading answer. This entry is the record that would otherwise not exist. The rule it argues for: **stage named files, never `git add -A`, in a repo where a second session may be mid-edit** — the same tree that made "merged" and "deployed" one event on 2026-08-12 makes "committed" and "somebody else's commit" one event too.
  - 35 new tests, 238 total, all passing.
- 2026-08-13: **A full security audit, and the ten things it changed.** Code, database, edge functions, gateway and host — probed from outside with the public key rather than only read. Findings and fixes are recorded in the private folder, not here, because this file is public; what follows is what changed, not how to exploit what was there.
  - **The one that was live:** the database accepted public self-registration, so anyone could create a working account. Closed. RLS held throughout — no record data was reachable — but the account itself gave away the staff list and could write rows. Now: accounts are admin-created only, and that was **verified from outside**, along with staff login and account creation still working.
  - **The database password was the placeholder Supabase ships in its example file** — the config named it twice and the last one won, so the real value never applied. Rotated onto a random value across all seven roles, generated on the server so it never entered a transcript. ~4 minutes of downtime.
  - **Rate limiting, which did not exist anywhere.** Password guessing is capped at the gateway (30/min, 400/hour **per visitor**, proven per-visitor and not global); form spam is capped in the database at 60 submissions per 10 minutes per address — deliberately loose, because carriers and hiring-event wifi put many real people behind one address, and it counts submissions rather than requests so reading a form or a dashboard costs nothing.
  - **Public forms stopped over-sharing and over-trusting.** A form's `config` — its review chain, stages and message templates — was readable by anyone; `/f/` now reads a `config_public` column with just intro/colour/icon ([#21](https://github.com/BlkTable/blktable.github.io/pull/21)). And a submission is now filtered to the questions that form actually asks the public, so a staff-only field can never be set from outside.
  - **Three permission gaps closed in the database, not just the buttons:** anonymous writes to the record tables removed, approve/reject/invite made manager-level in the DB as well as the UI, and photo signing now requires a real staff profile rather than any valid token.
  - **A bug that had not bitten yet:** *Restart form* was copying questions without their conditional rules, and carrying a config that still named the old form's fields. It would have silently un-gated Education details' three university questions.
  - **Two mistakes of mine, both caught and recorded:** a setting that briefly took staff login down, and a database test that proved nothing because of trust auth — the finding held on better evidence, but the first proof was worthless.
  - **Checked and healthy:** no XSS anywhere in four pages, RLS holding under a hostile session, and zero integrity problems across 124,000 submissions, 34,000 applications and 4,535 fields. Backups continue nightly, verified, copied off the server — one was taken and confirmed *after* the rotation.
  - **Left alone by decision:** SSH hardening. Login there is by password, so disabling passwords would lock the admin out; the safe order is a key first, passwords last. Less exposed than it was regardless — a guessed SSH password no longer reaches an unchanged database password behind it.
- 2026-08-13: **Two standards the events project needed, and both of them were gaps the app already had.** BLK Events was scoped on 1 August as a standalone app with its own Supabase project; reading BLKTable's code to check the fit reversed that, and narrowed it to two tables and five standards. Merged as [#22](https://github.com/BlkTable/blktable.github.io/pull/22) and [#24](https://github.com/BlkTable/blktable.github.io/pull/24), confirmed served on the live files.
  - **The app could not create a record.** Every row came from a public submit or an import; the `+` on the grid header adds a field, not a row. That is why an event Faisal has to create *before* anyone signs up had nowhere to exist — and why a phoned-in complaint never did either. Built for every table, on `can_edit`, reusing the record panel's own editors through one extracted `edFieldRowHtml()` rather than a second copy of an eleven-type if/else chain.
  - **A link per record instead of per table.** `config.parent` + `/f/?t=<slug>&e=<token>`. The design constraint that shaped it: anon has no read on `app_submissions` and must not gain one, so the page is handed only the fields the config names, and `config_public` says *that* the form is scoped without saying what to.
  - **The merge itself found the worst bug of the day, and it was mine.** The page was sending `p_token` on every submit. PostgREST resolves an RPC by the keys in the body, so a three-key body against the database's existing two-argument `submit_public_form` resolves to nothing — merging would have broken the submit on all 226 public forms, which is the 2026-08-09 outage in a shape a *merge* causes rather than a deploy. An ordinary form now sends exactly the two keys it always sent and the token is added only when the link carries one, so the code and the migration can land in either order. Two tests read that construction out of the page as source, since it lives inside `submitForm()`.
  - **Three smaller things, all found by writing the tests rather than by reading the code:** `new Date("2026-02-31")` is the 3rd of March, so a mistyped event date would have told everyone who opened the link a confident wrong day; `.invalid` had **no style at all** in `index.html` (only the public form has one), so pointing at a missing answer was invisible; and a required multi-select renders `.ed-checks`, not `.ed-in`, so it neither highlighted nor cleared.
  - **One pre-existing trap recorded rather than fixed:** a condition pointing at a *deleted* field hides its question forever, on the new panel and on the public form both, because each only reads answers for questions it rendered. The two copies agreeing matters more than either being cleverer — worth knowing before deleting a field from a table with conditional questions.
  - **A third standard the same day: a form that fills up** ([#25](https://github.com/BlkTable/blktable.github.io/pull/25)). `config.capacity` on the child, the numbers on the parent record, `open`/`full` as stage keys so the engine knows nothing about events. The parent row is locked `FOR UPDATE` across the count and the insert — a count read outside a lock is a count that *was* true, and two people on the last place must not both take it. `submit_public_form` returns jsonb so the confirmation can say "you have a place" or "you are on the backup list": a backup told only "thank you" turns up believing they are working. An empty capacity means no limit, the way every other optional config key behaves. Tightening the tests caught that a capacity edited below what is already booked would have read "-1 backup places left".
  - **Two more the same day: one signup per browser** ([#26](https://github.com/BlkTable/blktable.github.io/pull/26)) and **the swap** ([#27](https://github.com/BlkTable/blktable.github.io/pull/27)). One-per-browser is **opt-in and must stay so** — Job Applications can never have it, because a shared family phone, an internet café and a laptop passed round a room of applicants are all one browser, and refusing the second real person is far worse than a duplicate. The key is a random id the browser mints, *not* the `device` column, which is the User-Agent string and identical across every iPhone on one iOS version. Re-opening a link now shows where you stand, which is what replaces the cancel button we chose not to build. The swap promotes the oldest backup on a delete **by trigger, on DELETE only** — firing on a manual demotion too would re-promote the oldest backup into the place just freed and make a deliberate swap impossible — and refuses a promotion into full places under a lock rather than overfilling an event.
  - **A bug caught while writing the setup SQL, before any of it ran:** a record created from the dashboard has `status` null and *displays* as the first stage, Draft. The accept-check treated null as accepting, so **every new event's link would have been live the moment it was created**. When `open` is declared, null is now not one of the statuses that accept. Fixed in `11` and `12` at source rather than patched on top, since nothing had been applied yet.
  - **A third of the day's bugs were invisible-UI ones the tests only caught because they assert on classes and wording, not behaviour:** `.invalid` with no stylesheet rule, a required multi-select highlighted on the wrong element, `.sc-gold` which does not exist, and an impossible date rendering as a different day.
  - **The payroll export** ([#28](https://github.com/BlkTable/blktable.github.io/pull/28)) — `config.payroll = {date, group, rate, only_slot}`, and the only one of the six that needed **no SQL**: a manager can already read both tables, so the join is in the page. The range filters on the *event's* date because pay is earned by working it, and the amount sums each event's own rate rather than multiplying by one number. Every test in it is one of two failures — a row quietly dropped (a barista not paid) or a row counted twice (money out the door): both range ends inclusive, an event with no date in no period rather than every one, a non-numeric rate reading 0 rather than NaN, a deleted event earning nothing, and a nameless signup kept as `(no name)` rather than dropped.
  - **106 new tests** across six new files, 187 total, and all four pages parse and balance.
- 2026-08-12 (later): **The ⋯ on a record card opened a menu nobody could see — PR #20, merged and live.** Merged and deployed the same day it was reported again from the board; confirmed on the live file that `placeRecMenu` and the fixed-position menu are served. The menu was never broken: `wireRecMenu` fired, the class went on, `View` / `Delete` / the table's record actions were all in it. It was clipped. `.ja-card` is `overflow: hidden`, the kebab sits on the card's bottom edge, and `.rec-actions` opened *below* that edge — outside the box that clips it. List view was fine the whole time because `.ja-row` does not clip, which is why it survived this long. Now `position: fixed`, placed from the button's rect on open (checked first that no ancestor has a `transform`, which would have made `fixed` behave like `absolute` again), flipping above the button near the bottom of the screen, closing on captured `scroll` and on `resize`. One change in the shared `recMenuHtml`/`wireRecMenu`, so cards, rows, casting and every future table get it together. Card actions also got their empty slots back — the app's cards are `[action][View][action]` with fillers kept, but the custom card passed only the moves and the kebab, so a table with no statuses put its lone kebab in the *left* slot. **Also: #19 claimed 8 `flagTagHtml` tests and shipped none** (that commit touched `index.html` only), so the red "needs noticing" mark had no test at all — `docs/tests/flag-tag.test.js`, 9 tests, now covers it. The mark itself was correct in production all along: `config.flag = {field, equals: ["No"], label: "Not a student"}` on the yesno field, matched case-insensitively.
- 2026-08-12: **The conditional questions went live — the bug was the deploy, not the rule.** The Education details form was still asking all seven questions with "No" selected. Nothing was wrong with `condMet` or with the data: `show_if` was already correct on all four university questions in the live database, and the deployed page simply had no copy of the rule in it — PR #17 was still open while `main` had moved on through #18 and #19, and Pages serves `main`. Rebased the branch onto current `main` (clean, it was based on #15), 41 + 34 tests still pass, squash-merged as `aaff00e`, and confirmed on the live page that `condMet` is now in the served file. The `yesno` select submits `"Yes"`/`"No"` as its values while showing `Yes / نعم`, which is what the stored `equals: ["Yes"]` matches — so the gate closes on No and opens on Yes. Lesson worth keeping: for a form change, "merged" and "deployed" are the same event, and neither is "committed on a branch".
- 2026-08-11: **A question that is only asked when another answer says so — and the Education details form that needed it.**
  - Asked for a table: staff name (dropdown), branch (dropdown), date of birth read as an age, "did you go to university?" that opens a text box when the answer is Yes, major, and المعدل الجامعي من 4. Six of the seven questions were existing field types. The seventh was not a field type at all — it is a *rule about when a question applies*, and nothing in the app had one.
  - **Built as a standard, not as this form's special case.** `app_fields.show_if = {field, equals}` names another question on the same table and the answers that make this one apply. One `condMet()` decides it, and the public form, the record panel and the builder all call that one function. NULL means always asked, so every one of the 226 existing tables and every field on them behaves exactly as before — no backfill, no rewrite.
  - **A hidden question is not a blank answer.** It is not validated while hidden (a required question that does not apply would otherwise make the form unsubmittable), it submits nothing, and an answer typed before the gate closed — Yes, then No — does not travel with the submission. Same rule when staff edit: the row follows the answer live, and a stale answer is dropped on save.
  - **Except when the answer is history.** A record that *did* answer a question keeps showing it whatever the gate says now, because that answer is real and hiding it would lose it. It is the rule retired questions already follow.
  - **The builder had a problem worth naming:** a condition must point at a field id, and a question added in the same save has no id until it is written. The picker therefore references rows by a key that only exists in the page, and any condition pointing at a brand-new question is completed in a second pass keyed by position — one extra query, and only when a condition was actually set that way.
  - **Major and the GPA are behind the Yes as well**, which is one step beyond what was asked and the reason to say so: both were specified as ordinary required questions, and a required "major" asked of someone who answered "no university" is a form nobody can submit. One setting each to change back.
  - The staff and branch lists were **copied from the Health certificate form in SQL rather than retyped**, so the three forms cannot disagree about a person's name — but that is now the third copy of one list, and item 11 in Next steps (a shared staff/branch table) is the fix.
  - **41 new tests**, which load `condMet` out of *both* pages and assert the same answer from each, since the public form and the dashboard each carry a copy of the rule. Existing 34 still pass. DB: `workspaces/06-conditional-questions-and-education-table.sql`, applied and verified (7 fields, 292 names, 32 branches, 0 conditions pointing at another table's field, and `anon` can read the new column — a form whose conditional question never appears would be the failure).
- 2026-08-10 (later): **Workspaces, and the form is edited where the form lives.**
  - **OLD (Airtable) / New.** Everything migrated from Airtable is history; everything in live use is not, and the sidebar now says so — two collapsible workspaces above the category grouping, New open and OLD folded away. A table moves between them from its ⋯ menu. **Restart form** does the thing that makes this worth having: the table you have keeps every record and moves to OLD *frozen* (`is_active=false`, so its public form stops accepting submissions), while a fresh table with the same questions starts collecting in New under a new link — and the freeze only happens once the copy exists, so a failure can never leave the old form switched off with no replacement. The UI is **defensive**: until `app_tables.workspace` exists in the database no table reports one and the sidebar renders exactly as before, so the code and the migration can land in either order. SQL is written and waiting in the private folder (`workspaces/01-add-workspace.sql`) — additive, ends in ROLLBACK with two verification queries, and sorts tables by **provenance not name** (a migrated table has rows carrying `extra._import`).
  - **Editing the form moved into the Form tab**, next to the preview and the QR: Preview / Edit questions, with the questions inline — label EN/AR, type, options, required, staff-only, drag to reorder, remove, add. The old modal keeps only what is *not* the form (stages, record actions, review chain) and is renamed **Table settings**. It is the same widget and the same save path as the builder's, not a second implementation that would drift: the builder's state for that table is loaded so saving cannot wipe the stages and actions it also writes, its overlay stays shut, and one host lookup decides which of the two homes the question rows are built into.
  - **Still blocked on one thing from me:** the table-merging (209 tables that are ~85 processes split per year and per country) needs the live table list, and the live database is self-hosted where Claude cannot reach it. The runbook and the read-only inventory query are in the private folder (`merge-tables/`) — step 1 is one query and a CSV.
- 2026-08-10: **Airtable is empty — the last 30,558 records came out, and the "complete" of two days ago turned out to be a per-base illusion.**
  - **What the earlier check missed.** 2026-08-08's audit asked "has any row from this base landed?", so a base whose second table never came still counted as done. A per-**table** audit (`coverage_tables.py`, new) compares Airtable's 290 tables against `extra._import` (base id + table name on every imported row) and found **28 tables / 30,554 records** still outside — not the 4 tables I had reported. 24 of them were secondary tables inside bases the batch generator skipped by name: it skipped anything called "job application" or "DON'T USE" because those were the hand-built hiring table's territory, and took their *Internship Applicants*, *Junior Python Developer Applicants*, *AUB Job Applicants*, *Employee Information Form*, *Baristas*, *Countries* and *Talabat* tables down with them.
  - **The audit's first answer was wrong too, and wrong in the dangerous direction** — it reported "nothing missing". Probing with an empty `fields[]` parameter makes Airtable answer 422, and all 81 probes failed into a variable that was neither "has records" nor "empty". It now asks for a single named field, and **prints a loud line for any table it could not probe**, because a failed check must never read as a clean one.
  - **Imported in two batches through the same build → import → align → audit pipeline**, slugs generated rather than typed: **28,147 records / 31,939 files (33.4 GB)** from the four `DON'T USE` job-application tables, then **2,411 records / 1,823 files** from the other 25. **All 29 tables audit at zero differences.** Everything landed archived — searchable, off the dashboard, not servable as a public form.
  - **Two files failed out of 33,762.** One was recoverable and was fetched afterwards (an 11 MB JPEG, now in R2 under the exact key the importer would have used, so a re-run sees it rather than writing a second copy — `retry_failed_attachments.py`). **The other no longer exists in Airtable**: its attachment record has a null size and its freshly-minted URL 404s, so it was already lost there.
  - **859 imported applications look like they lost their photo and did not.** Their upload is a PDF (405), a video (238), HEIC/HEIF (98), a Word doc (28) or a RAW camera file (19) — every one is in R2 and reachable under "Imported from Airtable"; it simply cannot be a record's photo, because the photo field only ever points at something a browser can render. 91 more are genuine uploads named `web.whatsapp.com` and `www.facebook.com` — people attached saved web pages in 2021.
  - **Verified after the fact, not asserted:** the corrected audit re-run reports **0 tables with records outside BLKTable**, 238 holding rows, 52 empty in Airtable itself.
  - **Then counted row for row, because "present" is not "complete"** (`recount.py`, new — pages all 191,947 Airtable records back out and compares per table). **221 tables match exactly, 52 are empty on both sides, 0 tables are short for any reason other than the two below, and no table has fewer rows in Airtable than here.** The whole 34,113-row difference reconciles: **34,040** is the hiring dedup done by decision (photo-less and repeat applications) and **73** is drift, each of the 73 checked by record id and created *after* its import ran.
  - **The recount hit the slash bug a third time.** `quote()` defaults to `safe="/"`, so `Wastage (Branches/Date)` became a sub-path and 404'd — it now passes `safe=""`, and that table recounts 32 = 32. Worth remembering that this bug has now cost three separate scripts.
- 2026-08-10: **Airtable is still collecting — the migration is complete as a snapshot, not as a state.** The recount's "drift" turned out to be live traffic: the Airtable forms were never switched off, and people are still submitting through the links in circulation.
  - Since 5 August: **~365 job applications into `BLK - Job Application - 2025 August`** (the base name is misleading — it is the live Jordan intake), **~165 into `BLK - Job Application - 2026`**, **65 Contact Us**, and single figures into Franchisee, Employee Info, Customer Complaints, QC July and Contact us (Lebanon). Raw counts, before the dedup rule that would drop some of them.
  - **This is the thing to act on**: every day those forms stay up, BLKTable and Airtable diverge again, and re-importing is not free. Order of work: retire or redirect the public Airtable form links → re-run `recount.py` → top up the delta with the same importer (idempotent, so a re-run corrects rather than duplicates) → then the account can be cancelled.
- 2026-08-10: **The shell now looks and works like Airtable's, and the filter got the one thing it was missing.** The earlier parity pass (2026-08-09) did the inside of a table; this one did the frame around it, from a walkthrough of the live Airtable workspace.
  - **Theme rebuilt as two accents, not a palette.** Near-black surfaces separated by two weights of low-contrast line — every gradient and every `0 18px 40px` drop shadow replaced by a border, radii down from 11–18px to 6–8px. Blue (`#2d7ff9`) means "the primary action on this screen"; pink (`#f82b60`) means sharing, and marks the tab you're on. Nothing else is saturated.
  - **Every table has its own colour and glyph**, and it is the same square in the sidebar, on Home, in the base bar and in the palette. Derived from the table's key rather than stored, so it needed no migration and a table created tomorrow gets one; `config.color` / `config.icon` override it if anyone wants to pick.
  - **One search box, and it is a Ctrl-K palette** over every table you can see plus the open table's saved views, with arrow keys and Enter. The bell next to it is badged with how many records are actually sitting on your desk — the same number Home's "N for you" badges add up to — and clicking it takes you there. Help is a real shortcut sheet, not a link.
  - **The public form left the records view.** Each table now has a base bar with **Data / Form** tabs, and the QR block, link and regenerate controls live under Form. Every table was carrying that block above its records before. Task tables have no public form, so their Form tab is removed rather than shown empty.
  - **The grid reads as a spreadsheet.** Column headers are `type icon + field name` in normal case instead of shouty uppercase, every field type has its own glyph, and synthetic columns (Submitted, Branch, a score) resolve to the right one by name. A row number turns into a tick box on hover and offers an expand button; ticking rows offers **Export selected as CSV**, which is how you pull a specific handful of records out without filtering the whole table down to them. Plus row height (short → extra tall), a live record count, and a `+` at the end of the header row that opens a **field-type picker** and lands you in the editor with that question already waiting.
  - **Filter conditions can be grouped now** — `score > 80 AND (branch is Abdoun OR branch is Zarqa)` was not expressible before. A group is just another entry in the existing condition list, so **saved views written before groups exist load unchanged**: no column, no migration. The engine walks the tree recursively rather than special-casing one level, a group with no values is inert, and a group whose field gets deleted from the table is dropped with its condition. There is also a **Copy from a view** which offers only saved views that actually carry a filter. Table-agnostic, so Job Applications, Casting and all 177 custom tables got it at once.
  - **Home orders itself** by when you were last in each table (grouped Today / Past 7 days / Past 30 days / Older, the way Airtable's recents list does), by name, or starred-first. Last-opened is a per-person habit, so it lives in the browser rather than the database.
  - **34 unit tests** on the new pure logic — the period grouping, the colour/glyph derivation, and the filter tree — plus a syntax check and a markup-balance check on all four pages. Tests live in `docs/tests/` with a README, and they pull the functions out of `index.html` by name, so a rename fails loudly instead of silently going untested.
  - **What was deliberately not built, and why:** no Automations tab and no Interfaces surface — BLKTable has neither engine, and a tab that does nothing is worse than no tab. Same for Templates / Marketplace / Import in the sidebar footer, the Launch button, and the "upgrade to new forms" upsell. The create-view menu offers the three view types the app really has rather than Airtable's nine. No second table-tab strip, because the sidebar is our table navigation by design. Form cover image and logo are real work (storage plus rendering on three public pages), not styling, so they are a separate job. The applicant-facing form pages keep their own BLK branding — the walkthrough was of the internal workspace. Full list in the design spec.
- 2026-08-08: **The Airtable migration is finished.** 92 bases, 177 tables, 127,351 records, ending 00:29.
  - Ran as four chained batches, each generated from a live count of Airtable rather than hand-typed, and each built only after the previous finished — generated earlier, tables still importing would have been re-imported under a second slug and duplicated. Every table went through the same pipeline: build → import → categorise → align to Airtable → audit.
  - **All 177 tables verified identical to Airtable afterwards**, not merely at import time. Two flagged as "not clean" turned out to be stale slugs from batch lines I typed by hand with the wrong table name; the tables exist under the names the generator derived and both audit clean.
  - **19 tables archived rather than dropped** — the dead `DON'T USE` bases, Handover, Wastage. Imported in full and flagged `config.archived`, so they stay off the sidebar and Home behind one "Archived" line, and come back whenever wanted. Nothing was deleted to hide them.
  - Bugs the scale exposed, all fixed: a table named `Wastage (Branches/Date)` 404'd because `quote()` leaves `/` unencoded and the name became a sub-path (the same mistake as the R2 prefix slash); Airtable allows **two fields with the same name**, and the matcher kept only the first, so the second question matched the wrong field and would have been created again; Airtable also allows a field named `.` or `..`, which normalised to nothing and read as missing.
  - **Nightly backups are running unattended** — 02:35 on the 8th and the 9th, 43 MB each, verified and copied to R2.
  - Still deliberately out: the old job-application base (28,115 rows). It belongs in the hand-built `job_applications` table with its dedup and photo rules; a generated table would make a second, conflicting copy of the hiring data.
- 2026-08-07: **Made identical to Airtable, then given what Airtable's grid has — and finally backed up.**
  - **"Identical" turned out to mean two corrections to my own work.** I had split bilingual field names ("Phone Number - رقم الهاتف") into label + Arabic, which rendered as "English / عربي" — close, but not what Airtable shows, and it read as the question being asked twice. And retired questions from older versions of a form were rendered as if they were current, so a Complaints record showed both "Branch - الفرع" and "اسم الفرع؟". Questions now carry Airtable's name character for character, and a record shows today's questions plus only the older ones it actually answered. **All migrated tables audit at zero differences** — name, type, choices, order.
  - **What the audit found first time:** 23 choice questions had arrived as plain text boxes with their choice lists gone, 6 photo questions had no field at all (their pictures were only in the flat attachment list; 559 records now show them under the right question), 45 labels carried Arabic inside the English, and QC's 177 questions were in an order unrelated to the form. Root cause was in the table builder itself — `multipleSelects` mapped to `long_text` and discarded the choices — so it would have repeated on every future import. Fixed there.
  - **Two field types added:** multi-select (a `<select>` cannot hold "Clean, Organized", and forcing one pick would narrow what the form can record) and **linked records**, which is what made Barista Evaluation migratable — it is not a form but ten linked tables, 484 baristas with every quiz row pointing back at its person. A link shows the linked record's own name and opens it; its target id needs no lookup, because every imported row's id comes from the same deterministic rule.
  - **Migrated:** Contact Us 8,224 (three bases, 2022 to today), Barista Evaluation 1,426, and 17 more tables — Inventory Control, Product Feedback, Feedback فَض فِض, Employee Information, the surveys, Events. All audit clean.
  - **Grid features:** group by, summary line, CSV export, multi-column sort. Grouping reorders rows on screen, which broke the link between a row's index and the array it came from — clicking would have opened the wrong record. Caught and fixed.
  - **Backups existed nowhere.** Now nightly, verified, and copied off the server to R2 — and proven by an actual restore, not assumed.
  - **A correction worth recording:** I described the remaining Airtable bases as "mostly internal trackers". Counting them showed ~13,500 records of real operational and financial data I had written off — Stock Transfer 4,329, Petty Cash 2,587, an "Untitled Base" that is 2,384 delivery orders, Critical Expenses 1,623 (water, ice, electricity, Careem, Zain Cash), Remote Asset Tracking 1,214. I had judged them by their names instead of counting them.
- 2026-08-06 (latest): **Speed, scoring, and the cleanup those two uncovered.**
  - **Why the dashboard felt slow, measured rather than guessed:** the backend was never the problem (a 500-row page answers in 35 ms). Two things were. Photo signing made one request per photo, so a 300-row list queued hundreds of round trips six at a time — now batched, 300 photos in 2 calls. And the "thumbnails" were the full-size originals with CSS shrinking them: **a 150-card page pulled 238 MB**. 102,846 thumbnails later (96 minutes, 87–100× smaller) that page is **2.3 MB**. Opening a table also ran three queries in series before drawing anything; they now run at once and the first 150 rows paint immediately.
  - **Scoring is live and was validated before being switched on.** It reproduces Airtable exactly on every record of the current form (Mystery Shopper 1,066/1,066, QC 669/669). Older records keep the score their own form produced, because those forms scored out of 25 or 62 questions rather than today's 18 or 68 — recomputing them would rewrite the past and make years incomparable. Getting QC to 669/669 exposed a genuine Airtable quirk: a **blank** fridge temperature earns the point, because Airtable reads an empty number as 0 and `0 < 5` is true. Reproduced exactly so the history adds up, but it is scoring a non-answer as a pass and is worth fixing on the form.
  - **Every imported table had answers that were not really there.** A renamed or added question had no field to land in, so its answer sat in `extra._answers` — visible, but unfilterable and unscoreable. 61 questions across four tables are now real fields. That fix would have quietly polluted the live forms with dead and duplicate questions (`Countertops` beside `Countertops / Counterfront`), so each form is now aligned to today's Airtable form.
  - **Branch names collapsed onto one list**, 1,097 rows renamed, and the per-table hand-written mappings replaced by a shared resolver. Complaints and Praise could not be grouped by branch at all until 1,827 rows were promoted out of a question into the `branch` column.
  - **Two mistakes of mine, caught before they landed:** the label matcher stripped every non-Latin character, which collapsed all Arabic-only labels to one key and proposed mapping *"branch name?"* onto *"who is responsible?"*; and matching on the English part alone made bare `Zarqa الزرقاء` resolve to the Wink Street shop by discarding the Arabic that distinguished it. Both were found in a dry run, fixed, and the already-applied table re-checked (it was unaffected).
  - **11 photos cannot be turned into a thumbnail, and it is not a bug in the thumbnailer:** 9 were test files I left in the bucket during the R2 cutover (now deleted, each verified unreferenced first), 1 is an HTML error page that an expired Airtable link produced during the import, and 1 is **a QuickTime video an applicant uploaded named `.jpeg`** — which is why it never displayed as a photo either. Truncated uploads and 100-megapixel panoramas were also being refused; both now build.
- 2026-08-06 (later): **Mystery Shopper migrated, QC and Complaints importing, and the dashboard reworked for wide tables.**
  - **Mystery Shopper is in BLKTable**: 6,234 visits with 19,806 photos (36.3 GB) from six Airtable bases, spanning September 2022 to July 2026, one failed file. It became its own table of 112 fields (73 questions + 39 computed scores) and is **grouped by branch**, with all 34 branch names reconciled to one official list — 13 were the same branch spelled differently (Sweifeyeh, University St. 1/2, Bayader, Kia…), 5 were genuinely new locations (three Irbid branches, Hashemite University 2, Jal el Deeb in Lebanon). Three were judgement calls (Abdoun → Abdoun 1, Al Hurriya - Muqabalain → Al - Hurriyah, Zarqa → Zarqa Karama) and each is reversible, because every record still carries its original Airtable branch text.
  - **Shop Spot Check (QC)** is importing: 1,619 inspections, 22,522 photos, 52.7 GB — the widest form yet at 251 fields, 51 of them photos. **Customer Complaints** (1,439 from three sources) and **Customer Praise** (749) are queued behind it.
  - **Handover Sheets and Wastage are dropped** at your request. Handover alone was ~194 GB, so the remaining migration is now well under 100 GB rather than the original ~340 GB estimate — and the July decision to downscale old photos is moot, since it existed only for those bases.
  - **Job applications cleaned up**: applications with no photo (16,689) and repeat submissions from the same person (16,830, newest kept) were removed, taking the table from 67,507 to **33,988**. The duplicates were real Airtable records — one applicant had 721 submissions, 132 of them on a single day. ~16,800 photos in R2 are now orphaned, awaiting a decision.
  - **Dashboard**: grouping moved to the sidebar only; every form has a **⋯ menu** (open, edit, share, copy link, delete); a new **Table view** with sortable columns, a column picker and an admin "save for everyone" default; the record panel now shows choices as coloured pills, right-aligns Arabic, marks unanswered questions with an empty box, and — importantly — shows **every** photo on a record instead of just the first, which had been hiding up to 50 of 51 on a QC inspection.
  - **Saved views**: named views per table remembering their type, locked branch, columns, sort and filters, listed in the sidebar as "My views" and "Shared views". Personal views are private; **only admins can create shared ones**, enforced in the database.
- 2026-08-06: **All Jordan job applications are in — the table now holds ~67,500 applications, grouped by country.**
  - Imported overnight from the six Jordan bases: **67,308 records and ~57 GB of attachments, with one failed file** (a video whose connection dropped). Final split: **Jordan 67,291 · Lebanon 206 · Syria 8 · Iraq 2**, with 50,655 photos.
  - **Country grouping works the way you asked**: it fills itself from the phone's dial code as each record arrives, and corrects itself if a phone is edited. In the sidebar, *Job Applications* is a single line that expands to the country list with counts.
  - **Two bugs were caught before they became permanent.** First, Jordanian applicants who typed `0791234567` were being labelled **Lebanese**, because the importer defaulted to +961 for any non-Iraq base — that would have made ~67,000 people uncontactable, since the WhatsApp invite is Jordan-only. Second, Airtable's attachment links expire, and fetching everything up front then downloading for hours lost 4,592 photos on the first attempt; the importer now downloads each page's files immediately, and finished the remaining ~50,000 files with zero expiry failures.
  - **Phone numbers were cleaned up**: **2,935 applicants had written their number in Arabic numerals** (`٠٧٩٥٣٤٣٢٦٨`), which made them unusable — those are now proper numbers. Numbers typed twice into one field were salvaged, and 74 rows with no usable number at all were removed. **117 Jordanian numbers are only 7 digits long** — real digits but incomplete, so they were left for a decision rather than deleted.
  - **The dashboard was rebuilt for this volume.** Loading all 67k rows into the browser is impossible, so the board now works one year and country at a time, with counts and search coming from the database. A row-security check that ran once per row made the counts time out at this size; it now resolves once per query — **counts went from 8.3 seconds (failing) to 0.77 seconds, and search from 5.4 seconds to 0.46**.
  - Cards were tidied: every card is the same height with the same three actions in the same places — reject on the left, View in the middle, approve on the right. Deleting a form is a button on the form's own page again, instead of being buried inside the table editor.
- 2026-08-05: **First Airtable data is in — all six Lebanon/Iraq job-application bases are now live inside BLKTable.** Job Applications went from 31 records to **257**: 226 imported, with **353 attachments (5.2 GB, zero failures)** copied into R2 alongside them.
  - Written as a reusable importer (`blktable-migration/airtable-plan/import_job_applications.py`), **idempotent by construction** — each row's key is derived from its Airtable base + record id, so a re-run corrects rather than duplicates. Re-applying the same load left the count unchanged (85 → 85 on the pilot).
  - The agreed rules were followed exactly: no birthdays invented (Airtable stores an age, so the age is kept as given and the date of birth stays empty), original dropdown wording kept verbatim rather than squashed into our options, and only genuine outcomes mapped to Rejected — the 38 mid-pipeline applicants ("Phone interview", "1st 1on1", "Keep for later") stay New with their original stage preserved. 226 rows imported, 3 rejected, 0 approved.
  - Every original answer and file that has no home in our form is preserved and now **visible** via a new **"Imported from Airtable"** section in the review panel, with attachments as signed links (a 242 MB video streams correctly). Editing an imported record does not disturb any of it.
  - Phones normalised to `+961` so country, reviewer scoping and WhatsApp all behave; `photo_path` now only ever points at a format a browser can render (people had uploaded PDFs, a Word document and a HEIC into "Please Take a Selfie!" — those stay reachable as attachments instead of showing as broken images).
  - **Iraq's two bases are genuinely empty** (0 records), as are most "Content Creator" tables. The Jordan bases could not be touched: the available Airtable token only reaches 8 of 110 bases, all Lebanon/Iraq — the Jordan ones sit under a different Airtable account.
  - Two open items are listed in Next steps: 27 of the imported rows fall outside the Lebanon reviewer's phone-prefix scope, and 25 arrived with no phone number at all.
- 2026-08-05: **Cutover tail closed — every photo is now served from R2, and the new backend is taking real traffic.** The 10 in-flight application photos (the rows had already been re-synced) were copied across and verified by the strict test: every photo path the live database references, from all three sources, checked against the bucket listing — **85 referenced, 85 present, 0 missing**, each file checksum-matched, all 10 confirmed rendering through the real app path with the correct image type. A genuine job application then arrived at 07:36 on the new backend with its photo going straight to R2, which is the clearest confirmation the switch works end to end. Airtable scope was also settled: **forms and data only — Airtable users and permissions are not being migrated.**
- 2026-08-05: **CUTOVER DONE — BLKTable now runs on our own server.** The app talks to `https://db.blktable.blk.jo` and reads photos from Cloudflare R2; the cloud Supabase project is retired-in-place as a fallback.
  - Loaded and verified on the new backend: schema, config (40 tables / 299 questions / 31 branches), 28 job applications, 57 casting entries, 6 submissions, 14 chain log entries, 10 logins with their roles and 8 per-table grants, and the review-chain reassignment. Every content hash matched the old project exactly.
  - **Live smoke test after the switch** (not a rehearsal): a public form loads for an anonymous visitor, a submit succeeds, the photo lands in R2, the server-side trigger still records IP and device, `anon` still cannot read submissions back, an admin signs in and sees all records, and a **migrated** photo displays through a signed link. Test row and test files removed; R2 back to its exact baseline.
  - **Caught during the switch:** 10 job applications arrived on the old backend while the move was underway (yesterday 16:31–20:33 UTC). Their rows were re-synced and now match exactly (28 rows, identical hash). Their 10 photo *files* are still only in the old storage — the images won't display until they're copied to R2, which needs a read credential.
  - All 10 logins were recreated with temporary passwords (auth users cannot be transferred between Supabase instances) — **everyone should change theirs on first sign-in**. Existing QR codes and form links are unaffected.
  - Also mapped the Airtable replacement work: 110 bases collapse to 85 processes; decision sheets written (see Next steps).
- 2026-08-05: **Self-host cutover prepared end to end, and photo storage moved to Cloudflare R2.** Cloud is still live and unchanged; this was all preparation plus one real data move.
  - **Migration bundle built by introspecting the live cloud database** (not hand-written) and rehearsed on the real self-hosted server inside a rolled-back transaction: schema (11 tables, 22 constraints, 23 indexes, 14 functions, 5 triggers, 34 RLS policies, all grants incl. 26 column-level), config data (40 tables / 299 questions / 31 branches), submissions (18 job applications, 57 casting, 6 submissions, 14 chain log entries) and the people layer (10 roles, 8 per-table grants). Fingerprint-compared to cloud: every object and every row hash matched.
  - **Auth users can't be migrated** (per-instance password hashes, new ids), so the people file matches everyone **by email** and **remaps the review chain** — the old user ids stored on records are rewritten to the new logins, verified by hashing chain ownership by email so it proves the right *person* lands back on each record, not just a resolvable id. A missing login warns by name and skips only that person; the file is re-runnable.
  - **Photos: all 86 objects / 125 MB copied to R2**, integrity-checked per file and re-verified with an independent listing (0 missing, 0 size mismatches, 0 failures). Objects keep their original names under a `photos/` prefix, so **no stored `photo_path` needs rewriting**. The 11 orphaned files came along rather than being silently dropped.
  - **New `r2` edge function** on the self-host (upload / sign / delete / health), tested through the public URL: anon upload, 9 MB accepted, 13 MB rejected, non-image rejected, signing refused without a session, presigned fetch byte-identical, tampered signature and path traversal rejected, delete then 404. **Uploads are proxied through it, and viewing uses presigned urls, so the R2 bucket needs no CORS rule at all** — one less external dependency.
  - **Front-end is R2-ready but not switched:** all four pages now call a storage shim (`storageUpload` / `storageSignedUrl` / `storageDelete`) behind one constant `STORAGE_MODE`, shipped as `"supabase"` with each supabase branch kept verbatim, so **live behaviour is unchanged today**. Cutover = set `SUPABASE_URL`, `SUPABASE_KEY` and `STORAGE_MODE` in `index.html`, `apply/`, `cast/`, `f/`.
  - Also: found and fixed a bug where the function sent the caller's session token in the `apikey` header, which the gateway rejects before auth is checked — every staff request looked unauthorized. And a hardening note recorded privately: on a fresh self-hosted stack, newly created tables are granted everything to `anon` by default, which would have quietly defeated the column-level least-privilege on the public form tables; the schema file now revokes that before granting narrowly.
- 2026-08-04: **New table: Non-conviction certificate / شهادة عدم محكومية** — an exact clone of the Health certificate form, created as data only (no code change). `app_tables` id `77fd4b75-9dcd-4a86-859a-aa837a424ee9`, slug `non-conviction-certificate-4110`, form link `https://blktable.blk.jo/f/?t=non-conviction-certificate-4110`, `kind='form'`, `is_active=true`, empty config (no stages / actions / chain layers) — same as Health certificate. Same 3 required questions in the same order: **Name / الاسم** (dropdown, 292 staff) and **Branch / الفرع** (dropdown, 32 branches) with both option lists **copied verbatim from the Health certificate fields** (verified byte-identical with `is not distinct from`, so the two forms can never drift apart by hand), plus **Non-conviction certificate photos / صورة شهادة عدم محكومية** (photo). Access mirrors Health certificate: HR (`hr@blk.jo`) view-only — `can_edit=false`, `can_manage=false` — confirmed by simulating HR's JWT (`can_access` true, edit/manage false, 3 fields visible). Verified anon can load the form and submit to it (rolled-back insert test). One deliberate difference: `name_ar` is set on the new table so the form title reads "Non-conviction certificate · شهادة عدم محكومية", where Health certificate's is null and shows English only — worth backfilling on Health certificate for consistency. NOTE: keeping two hand-copied staff lists in sync is a known trap — a shared "staff list" source is the real fix if more of these document forms appear (see Next steps).
- 2026-08-04: **Extra questions can be positioned on built-in forms (table-agnostic standard).** Admin-added "extra" questions on Job Applications / Casting always rendered in a block at the very bottom of the public form. New nullable `app_fields.after_field` anchors a question to a built-in one: the builder (built-in mode only) shows a **"show after"** picker listing that form's built-in questions, and `/apply/` + `/cast/` insert each extra right after its anchor's `.field` wrapper — empty/unknown anchor keeps the old end-of-form behaviour, and several extras on the same anchor keep their configured order. Hiding logic and placement now share one `builtinWrap(col)` helper per form. First use: the new job-form question was reworded **"Where did you worked before ?" → "Where have you worked before?"** (Arabic `وين اشتغلت من قبل؟`) and moved to sit right after *Favorite drink at QahwaBLK*. 6 unit tests on the placement order + inline JS syntax check; verified anon `get_extra_fields` returns `after_field` and both live forms carry the code. Commit fc966cb. Not changed: the review detail modal still lists extras after the built-in fields.
- 2026-08-03: **Manager access actually works now (was admin-only in disguise).** HR (`hr@blk.jo`) was set as a *manager* on Job Applications but still couldn't approve, reject, send the WhatsApp invite, or delete — because those controls were gated on `isAdmin`, not on the manager flag, and the DELETE RLS policies were `is_admin()` only. Fixed at both layers as a table-agnostic standard: **UI** — the Approve/Reject/Send-WhatsApp/Restore/Delete buttons and the "send to all unsent (N)" bar on Job Applications now gate on `canManage('job_applications')`; the per-record `⋯` menu takes a `mayDelete` argument so Casting passes `canManage('casting')` and every custom table passes `canManage(table.id)`; the "Change photo" button moved from admin-only to `canEdit(<table>)` (editors could already change every other field). **DB** — new `can_manage_table(key)`, `can_edit_table(key)` now treats manager as implying editor, and the DELETE policies on `job_applications` / `casting_applications` / `app_submissions` are `can_manage_table(...)` (job applications still AND `ja_scope_ok(phone)`, so a country-scoped manager can only delete inside their scope). Verified by simulating HR's JWT: manager+edit true on Job Applications, false on Casting, and both UPDATE and DELETE pass RLS (tested in a rolled-back transaction). Permission ladder is now explicit and documented in code: **view < edit < manage**, and the Users/Share checkboxes read "manager (full control)" with a tooltip. Not changed: IP/device fingerprint stays admin-only, and table creation/settings/QR-rotation/Users panel stay admin-only.
- 2026-08-03: **Table categories (table-agnostic standard).** `app_tables.category` now drives grouping in the sidebar (collapsible `▸ OPERATE 33` header with a count, collapsed by default, remembered per user in `localStorage.blk_cat_collapsed`) and on Home (cards under category headings with counts). Uncategorised tables render exactly as before — when no category is in play the Home layout is unchanged. Task tables (`kind='task'`) get a `Task` badge on their Home card and their public-form QR/link bar is hidden, since they are not public forms. 8 unit tests on `groupByCategory` (ordering, trimming, whitespace-only categories, null input) + full inline JS syntax check. **Correction to the Operate import entry below:** the imported tasks were NOT invisible to admins — `is_admin()` RLS returns every row and `loadCustomTables` filters on `builtin_key`, not `is_active`, so an admin saw 33 loose sidebar items; this grouping is what tidies that. Reviewer invisibility (no `table_access` grants) and public-form blocking (anon RLS requires `is_active`) were correct as stated.
- 2026-08-03: **WhatsApp interview invite is now Jordan-only.** The Arabic invite text and the fixed BLK location are Jordan-specific, so the invite is gated on one helper `waEligible(a)` (phone starts `+962`, via the existing `phoneCountry()`): the Send WhatsApp/Resend button only renders on approved cards and rows for Jordanian numbers, the "Send to all unsent (N)" bar counts and opens Jordanian applicants only, and the approve/schedule popup says so for everyone else (approving itself is unchanged). `waUrlFor()` returns null and `sendWhatsApp()` refuses with a named-country message if called on a non-Jordanian record. Non-Jordanian approved cards show a muted "WhatsApp invite is for Jordanian numbers only" note. 8 unit tests on `waEligible`. Note: a phone without a `+<code>` prefix (only possible via hand-editing in review) counts as non-Jordanian — same rule that already decides the country flag chip.
- 2026-08-03: **Job application asks Lebanese-number applicants if they're Lebanese.** New nullable `job_applications.is_lebanese` text column (anon INSERT / authenticated SELECT+INSERT+UPDATE grants, verified by an anon insert test). On `/apply/` a Yes/No "Are you Lebanese? / هل أنت لبناني؟" question sits right after City and is shown **only when the phone country is Lebanon (+961)** — same conditional pattern as the Jordan-only City list; required when shown, cleared and saved null otherwise. Review side: shown in the detail modal, inline-editable (Yes/No), added to the Job Applications filter as a yes/no condition, and listed in the built-in form editor so admins can untick it. Note the country restriction (previous log line) still scopes by phone prefix, not this answer. Commit f7b68b7, live and verified on blktable.blk.jo/apply/.
- 2026-08-03: **Operate tasks — foundation + full import (data only, no UI yet).** Read the user's Operate export (`operate-import/qahwa_blk_active_tasks_export.json`, 33 active tasks) and wrote a full readable digest (`operate-import/01-active-tasks-digest.md`, every task/question/assignment). Three additive migrations on `cisqemycewkqakyqmusw`: (1) `branches` table (31 branches + stable short `code`s, RLS: authenticated read / admin manage), `app_tables.category`, `app_tables.kind` (`form`|`task`, checked), and task-instance columns on `app_submissions` (`branch`, `due_at`, `window_end`, `done_at`, `generated`) with a unique index on (table, branch, due slot) where generated; (2) branch seed; (3) the import itself — **33 task tables + 249 questions**, category `Operate`, `kind='task'`, `is_active=false` so they're invisible in the live app and can't be served as public forms. Question types mapped YES/NO+Checkbox→`yesno` (118), Media→`photo` (59), Numeric→`number` (42), Text→`short_text` (29), Drop-down→`dropdown` (1). Per-question branch scoping preserved in `options.branches` (branch codes; absent = all 31). Verified row-by-row against the export: every task's field count, frequency, interval and branch count matches. Import SQL is committed under `operate-import/` and is idempotent. Confirmed with user: hourly = every N hours; thresholds wanted (new, Operate has none); assignment target = branch (2 accounts/branch, logins later); clean start, no history migration.
- 2026-08-03: **Per-reviewer country restriction (row-level scoping).** A Lebanon reviewer (Pope Harout, `popeharout@blk.jo`) needed to see only Lebanese job applications, not all. Added a nullable `table_access.scope jsonb` (null = full access, unchanged for everyone); first scope type `{"phone_prefix":"+961"}` ("Lebanese" = phone starts +961, the only country signal — no nationality column). Enforced in the DB via `ja_scope_ok(phone)` in the `read`/`write` RLS policies on `job_applications` (verified by simulating Pope's JWT: he sees only +961 rows, admins see all). UI: the Users panel table-access rows now show a **country dropdown** (All / Lebanon / Jordan / Syria / Iraq) for scopable tables (`SCOPABLE = {job_applications}`), saved into `scope`; the per-table **Share** modal preserves existing scope so it can't silently wipe it. Note: only Job Applications is scopable for now (Casting/custom would each need their own RLS scope check). No Lebanese applications exist yet, so Pope's list is empty until +961 people apply.
- 2026-07-29: Fixed desktop form-submit bug (native page-reload submit → JS AJAX on all three public forms). Home assignment badge/filter + delegate-return chain routing. Form-manager per-table access. Created this STATUS.md (the save ritual's required file, missing until now) and committed it.
- 2026-07-29: Shipped "assigned to me" separation on custom tables — "For you" tag + gold edge on entries you're the current assignee of, an "Assigned to me (N)" toolbar toggle (filters list + stage tabs + count), and Home cards now open filtered to your entries when they have any. Pushed & live (commit 3ef482d).
- 2026-07-30: Discussed & proposed the chain workflow upgrade (auto 3-stage board + return-to-assigner sign-off + realtime) — see Next steps #1. Not built yet; awaiting final go. Verified realtime publication is empty and Sample Request Form is the only chain table (2 layers).
- 2026-07-30: **Made the Airtable-style condition Filter the ONE standard filter on every table.** Was custom-tables-only; Job Applications had a separate hardcoded gender/age/city panel and Casting had none. Refactored the engine to run off a per-table "filter view" descriptor (`getFields`/`getVal`/`records`/`rerender` + DOM ids) so the SAME builder now drives Job Applications, BLK Casting, and all custom tables — each table's conditions come from its own questions (built-in core columns + admin "extra" questions, minus hidden ones; a synthetic **Age** number field replaces the old age-range). Removed the bespoke job filter (HTML + wiring). Engine logic unit-tested (9 cases) and full inline JS syntax-checked. Table-agnostic → future tables get it automatically.
- 2026-07-30: **Fixed public-form submit failing on PCs** ("new row violates RLS" / "permission denied"). Root cause: PCs where a staff member is logged into the admin app submit as the `authenticated` role, which was blocked at BOTH layers — the public-submit RLS policies were `TO anon` only, and `authenticated` also lacked the table-level `INSERT` GRANT. Two Supabase migrations: (1) added `authenticated` to the public-submit RLS policies on `app_submissions`, `casting_applications`, `job_applications` + the `storage.objects` photo-upload policy; (2) `GRANT INSERT` on the three form tables to `authenticated`. Verified via `SET ROLE` that both anon and authenticated can now submit. (Phones = anon, always worked.)
- 2026-07-30: **Review-photo upgrades (table-agnostic, all current + future tables):** (a) click any attachment photo → full-screen lightbox (Esc/click to close); (b) admins get a **Change photo** button in the review panel that uploads a replacement and rewrites the record's photo reference (JSON field for custom tables, `photo_path` for job/casting); (c) **Submitted IP + Device are now admin-only** — routed through one `fpMeta()` helper gated on `isAdmin`, hidden from reviewers and managers. NOTE: #c is UI-level only; managers share the `authenticated` DB role so a direct query could still read those columns — DB-level hardening (is_admin() view / side table) is an open follow-up if wanted.
- 2026-08-02: Added a **form intro** as a table-agnostic standard (`app_tables.config.intro = {en, ar}`) — builder gets EN/AR intro boxes (multi-line, draft-saved), and all three public form pages render it under the title; built-ins get it via `get_form_config`. Created the **Hackathon in the garage** table (10 questions, bilingual, slug `garage-day-7q3m`) directly in Supabase; verified anon can read the table, all 10 fields and the intro.
- 2026-08-02: Two more table-agnostic standards, both driven by this table's needs: a **`dob` field type** (asks a birth date, reads as age everywhere, filters as a number Age — brings the Job Applications age behaviour to every table) and **"other" dropdown choices** (`|other` flag on a choice opens a required free-text box on the form, stored as `<fieldId>__other`, shown and editable in review). Round-tripped through the builder's Options box. 27 unit tests on the new helpers, all inline JS syntax-checked.
- 2026-07-30: Shipped a per-field **Filter** on all custom tables (first as a faceted panel, then rebuilt as an Airtable-style condition builder with And/Or). Made public-form dropdowns a **searchable combobox** for big option lists. Builder Options now accept one-per-line/comma (fixes Arabic RTL paste). Added a Branch dropdown (32 branches) + an employee-name list (292). **Fixed a desktop-only submit bug** the combobox introduced (typed-not-clicked value left required fields empty → blocked on desktop, worked on phone). Commits e9cc9f3 → f44e464, all pushed & live. NOTE: JS syntax-checked via VS Code's bundled Node (`ELECTRON_RUN_AS_NODE=1 Code.exe -e …`) since Node isn't installed standalone.
- 2026-08-10: **Job Application: Country is asked before the phone, and it now decides the dial code.** The country picker on the phone row was an independent choice an applicant could contradict (pick Lebanon, type a +962 number), and it sat *below* the phone field it governed. Country now comes third, right after Date of birth, and the phone row shows a **read-only** flag + dial-code prefix that is pure output of that answer — it drives the dial code, the number format, the digit-length check, and the two conditional questions (Jordan-only City, Lebanon-only "Are you Lebanese?"). The picker button and its dropdown are gone. **Syria and Iraq were removed** from the form (both the Country options and the +963/+964 entries) — they were wrong to offer. Review side keeps them so the ~existing Syrian/Iraqi records still render, group and filter as before. `cast/index.html` still has the old adjustable picker; not touched.
- 2026-08-17: **A record opened underneath the top bar, with its close button behind it** (PR #38, live). Every record panel is a `.modal-overlay` at `z-index: 50`; the top bar is sticky at `z-index: 60`, so the bar painted over the panel's first ~110px — exactly where the record's name and the ✕ sit. One cause, three symptoms: the panel looked off-centre and too high, its top looked cut off, and the close button appeared not to exist. Fixed in the shared modal rules, so it holds for every form's record panel and for the QR / share / users / builder / schedule panels: overlay **50 → 100** (above the bar and the profile menu, below the lightbox and the pop-up menus meant to sit over a panel); **`margin: auto`** on the card centres it while there is room and collapses when there is not, where `align-items: center` would have pushed the top of a long record out of the scroll area for good; and a record now **scrolls inside its own card** (`max-height: 100dvh - 48px`, `#modal-body` scrolls) so the ✕ stays in the corner at question 60 of 171 instead of scrolling away — the ✕ is a round button now, not a bare glyph. **Escape closes whichever panel is on top**, by clicking that panel's own ✕ rather than calling a list of close functions, so each keeps its own teardown (the record panel's autosave listener) and any panel added later is covered without touching this; the lightbox keeps its own Escape and sits above, so a photo closes first and the record stays open. Verified with the 13-file test suite green, an inline-JS syntax check, and a headless render of the real stylesheet with the top bar in place (short record centred and clear of the bar; long record capped at the viewport with the ✕ pinned). Note on how it was landed: another session held uncommitted work in `index.html` at the time, so only these hunks were staged and pushed as their own branch via `git commit-tree` — the other session's working tree was never touched.
- 2026-08-18 (cards): **Closed PR #39 and replaced it with card customisation, as decided.** #39 restyled the three public form pages (`apply/`, `cast/`, `f/`) to look like Airtable's forms; it was closed unmerged — branch kept — and the work aimed at the internal workspace instead, where Airtable's equivalent is a gallery view's "Customize cards". Shipped as **PR #44**: cover question or none, crop vs fit, which fields and in what order with the first as the title, and field names on or off; `Branch` and `Submitted` offered alongside the questions, since the cards already understood them but nothing could ask for them. Two levels of storage, copied from the column picker beside it — your own choice in `localStorage`, an admin's **Save for everyone** into `app_tables.config` (`card_fields`, `card_cover`, `card_cover_fit`, `card_labels`; the old hand-set `card_no_cover` still means "no cover" and is retired on a table when the new default is saved). **What was already there was the storage, not the choice:** `config.card_fields` was readable but no screen ever wrote it, so changing a card meant editing a config row by hand. One fix reaches wider than the panel: `input { width: 100% }` is set page-wide for the form fields, and inside these flex labels it stretched every **tick box to the full width of the panel** and pushed its wording to the far right — in Columns, Group and Color already, not just the new panel; it is now scoped away from checkboxes and radios in `.cols-panel`. **383 tests pass** (was 333): 28 on the rule (`docs/tests/card-customise.test.js` — which of the three levels wins, a cover naming a deleted or no-longer-file question, an empty list read as "nothing chosen" rather than an empty card, `"None"` surviving despite being stored as `""`), and 22 on the panel **clicked** in headless Chrome (`docs/tests/card-panel.chrome.js` — the arrows, the last field that cannot be unticked, Crop/Fit, the cover radios, Reset; the only test here that needs a browser, and it skips rather than fails without one). Also rendered against the real stylesheet to look at. Deliberately not done: drag-reorder (arrows are keyboard-reachable and far lighter), and storing the choice **per saved view** rather than per table — the natural next step, and a change to `applyView`/`saveCurrentView`.
- 2026-08-18 (cards, merged): **PR #44 merged as `9493305` and live.** Squash-merged, branch deleted, GitHub Pages rebuilt about 90 seconds later. **Verified on the served file rather than assumed** — `https://blktable.blk.jo/index.html` is 561,397 bytes (was 546,295) and contains `id="card-btn"`, `renderCardPanel`, `.ja-card .photo.fit`, the scoped `.cols-panel input[type="checkbox"]` rule and the `card_cover_fit` key. PR **#39** stays closed and unmerged by decision; its branch `design/airtable-form-parity` is kept on the remote if any of it is wanted back. The build worktree `C:\Users\ASUS\blktable-cards` has been removed; the shared clone was never touched and is still on `fix/phone-countries-clobber` with a clean tree.
- 2026-08-19: **An answer that has to be TOLD about, and everything needed to send it by itself.** Kitchen food safety asks a `Status` dropdown (Accepted / Rejected) and a rejected batch was an answer somebody had to happen to look at. It is now a **standard on every table** — `config.alerts = [{field, equals, label, template, contacts, send}]`, beside `config.flag` / `statuses` / `actions` — set in the table editor: pick the question, the answers that trip it, the message and the numbers. A tripped record carries a red **⚠ chip** on its card, row and header, and a banner with one button that opens WhatsApp with the message already written, to whoever you choose (any typed number, or the saved people). **Notify is offered on every record in every form**, rule or no rule, so it works on the 226 forms nobody has written a rule for. Two limits are said in the UI rather than hidden: the page cannot confirm delivery, and an upload is *named*, never linked, because a signed R2 url is dead within the hour. **The automatic half is authored and waiting on Meta credentials, not on code**: `whatsapp/02-notify-outbox.sql` in the private folder installs `notify_outbox` + a row trigger on `app_submissions` (the only place that sees all four write paths — public RPC, reviewer PATCH, create-record RPC, generated rows) + `notify_claim`/`notify_mark`/`notify_test`/`notify_retry`, and `selfhost/functions/notify/index.ts` is the Cloud API sender. It installs with `notify_config.enabled = false`, so real rejections queue **real, addressed, fully rendered messages that go nowhere** — the dry run is the product, and go-live is one `UPDATE`. Eight guards, each for a named failure: the trigger cannot break a submit on 226 forms (whole body in an exception block), only a *transition* fires, no duplicate inside `dedupe_minutes`, a per-rule hourly ceiling that writes **one** visible marker instead of 5,000 WhatsApps, generated and stale rows never fire, claiming is `FOR UPDATE SKIP LOCKED`, and `/drain` needs its own secret because the anon key is public. Waking it is a once-a-minute cron as `ali` (no sudo, no `pg_cron`); if `pg_net` turns out preloaded the trigger can ping instantly and the cron becomes the safety net. Visibility is in the app: **Alert messages** on the record (sent / queued / failed with the reason, and Retry), **Send a test** for managers, and a chip beside the record count reading "⚠ 2 alerts not sent" / "3 waiting". Every one of those reads survives the table not existing yet, so the app can ship before the SQL is applied. **Also fixed, found on the way:** the table editor was writing `config` as a *replacement* of four keys, so saving any table silently wiped the other sixteen — `parent`, `flag`, `capacity`, `score_field`, `card_fields`, `table_columns`, `detail_fields`, `filter`, `payroll`, `colour`, `icon` and the rest. It now merges. **Checked, not assumed:** recipients are not public — `anon` may read only `config_public`, a generated whitelist column (`intro`, `color`, `icon`, `scoped`, `one_per_device`), and it reads `{}` for this form on the live database. 38 tests in `docs/tests/alerts.test.js`, whole suite green, all inline JS parses. Runbook and go-live checklist: `whatsapp/00-DESIGN-automatic-whatsapp.md` (private folder — it names numbers and server details).
